# Changelog

2026-04-26 [nba][etl] Env-driven `AZURE_SQL_TRUST_CERT` extended to the two NBA-relevant raw-pyodbc gate scripts, mirroring the `db_inventory.py` change from commit `1dcc9a0` and the SQLAlchemy 5-script change from commit `5e5d5df`. Two-line edit per file: a new module-level `TRUST = os.environ.get("AZURE_SQL_TRUST_CERT", "no")` constant added next to the existing `SERVER`/`DATABASE`/`USERNAME`/`PASSWORD` block, and the `"TrustServerCertificate=no;"` literal inside `CONN_STR` replaced with `f"TrustServerCertificate={TRUST};"`. Files: `etl/gate_check.py` (used by `pregame-refresh.yml`), `etl/game_day_gate.py` (used by `nba-game-day.yml`).

Bit-exact-default verified per script: each module was imported under a stub environment for `AZURE_SQL_*` and its `CONN_STR` constant inspected in three modes (TRUST_CERT unset, =no, =yes). Default and explicit-no produce the byte-identical pre-edit string; opt-in flips only `TrustServerCertificate=no` to `=yes`, leaving `Encrypt=yes;` and `Connection Timeout=90;` untouched. No real DB connections were opened during verification.

Live end-to-end test against the local Mac container with `AZURE_SQL_TRUST_CERT=yes` and the `AZURE_SQL_*` aliases sourced from `/Users/schnapp/sql-server.env`: `gate_check.py` returned `true` (4 pregame games today on the NBA Sunday playoff slate); `game_day_gate.py` reported `pregame=4 live=0 newly_final=0 minutes_since_grade=520 interval=14 run_odds=true today_et=2026-04-26`, with all six GitHub Actions output flags consistent. Both scripts now run unmodified on either Azure SQL (default) or the local container (`AZURE_SQL_TRUST_CERT=yes`).

Holdouts after this commit: 3 raw-pyodbc-DSN scripts still hardcode `TrustServerCertificate=no` and remain Azure-SQL-only — `etl/lineup_cleanup.py`, `etl/cleanup_stale_odds_and_grades.py`, `etl/seed_user_codes.py` (all utility/admin scripts with low Mac-mirror value). The two NBA-day workflows themselves (`pregame-refresh.yml` and `nba-game-day.yml`) are now unblocked for Mac mirroring as a future step; their orchestration steps run unchanged, the only thing that was preventing a Mac mirror was the hardcoded trust-cert in these two gate scripts. No production behavior change on the VM runner: every existing scheduled and dispatch run uses the default `no` path and produces the bit-exact prior connection string.

2026-04-26 [nba][grading] ADR-20260425-3 close-out. The immutable-corpus architecture is fully live in production. Chain run `24933461522` was cancelled 2026-04-25 at 20:52 UTC after reaching 135 of 143 target ADR-6 dates (94.4 percent, 8 dates short of the natural-stop condition); cancellation did not block the architecture transition because the deferred Path-1 work and the weekly recalibration job ship independently of backfill completeness. The remaining 8 ADR-6 dates are an analytics enhancement on `common.player_tier_lines` (the 4 hit-context columns), not a calibration input; the weekly calibrator fits from a 30-day rolling window of resolved tier-line outcomes regardless of which historical dates carry the new ADR-6 columns.

Path-1 shipped as commit `0f118b1` on 2026-04-25. The block of `grading/grade_props.py` that previously called `fit_calibrator(engine, as_of_date=grade_date_str)` on every grading invocation was replaced with a read from `common.grade_calibration` that interpolates by bucket midpoint and clamps with the global `max_well_sampled_rate` output cap. Identity calibrator is the fallback if the table is empty (e.g. a fresh DB before the first weekly run). Verified live: today's nightly grading.yml run `24954638236` (10:40 UTC, workflow_run-triggered) logged `Calibrator loaded from grade_calibration: 19 buckets, cap=0.7912621359223301` twice. Daily grading no longer refits the calibrator on each invocation.

`weekly-calibration.yml` is operational on schedule. Four runs to date, all successful: three manual dispatches between 23:16 and 23:31 UTC on 2026-04-25 (sanity dispatch, dry-run check, then the first real fit that populated `common.grade_calibration` with `max_well_sampled_rate` and wrote the inaugural snapshot to `common.grade_calibration_history`), and today's first scheduled cron run at 07:14 UTC (Sunday 06:00 UTC schedule, runner picked up the queued job ~74 minutes after the cron fired). Post-BACPAC inventory of `common.grade_calibration` from the Mac container snapshot: 19 buckets, every row carries `max_well_sampled_rate = 0.7912621359223301`. `common.grade_calibration_history` carries 17 bucket rows from the first snapshot date 2026-04-25.

`MODEL_VERSION` stamping is forward-going as designed. As of the 2026-04-25 23:40 PDT BACPAC snapshot: 2,770 rows in `common.daily_grades` carry `model_version = 'adr-20260425-3'`; the remaining 1,232,364 rows are NULL = pre-stamp legacy, exactly per ADR-3's stamp-forward-no-backfill decision. New rows from each daily grading run carry the stamp; the immutable corpus principle is in effect for the active model version.

Three loose ends, none blocking: (a) the 8-date ADR-6 hit-context gap in `player_tier_lines` could be filled by a targeted one-off backfill but nothing depends on it; (b) the `/transparency` page is still seeded `disabled` behind the `page.transparency` feature flag with `sb_unlock=go` admin-cookie bypass, ready to flip when the data depth feels presentable; (c) no further chain-redispatch wiring remains in `grading.yml` since the architecture no longer requires walk-forward refits. See ADR-20260425-3, `grading/grade_props.py`, `grading/weekly_calibration.py`, `.github/workflows/weekly-calibration.yml`. Commits: `0f118b1` (Path-1), `e87c70b` (MODEL_VERSION stamp), `987c102` (two-cap fix), `a8b1068` (weekly job + workflow), `f5d97a6` (`/transparency` page).

2026-04-26 [shared][etl][infra] Second write-path workflow migrated to the Mac runner, and the env-driven `AZURE_SQL_TRUST_CERT` knob extended to every remaining SQLAlchemy `get_engine` builder in `etl/`. Two commits: `5e5d5df` (uniform 5-script change), `cf89f5b` (`compute-patterns-mac.yml`).

`5e5d5df` applied the same one-line change to `etl/compute_patterns.py`, `etl/migrate_common_teams.py`, `etl/nba_clear.py`, `etl/nba_etl.py`, and `etl/signal_backtest.py`: the literal `&Encrypt=yes&TrustServerCertificate=no` in each `get_engine` is now an inline f-string reading `os.environ.get('AZURE_SQL_TRUST_CERT', 'no')`. Default `'no'` preserves the prior Azure SQL behavior bit-exact; opt-in via `AZURE_SQL_TRUST_CERT=yes` flips only that field, leaving `Encrypt=yes` in both modes. Verified per script by importing each module under a stubbed `sqlalchemy.create_engine` and inspecting the URL it would have constructed; no real connections were opened during verification, and every script produced the bit-exact prior connection string under the default. With these five scripts plus `etl/db.py` (shipped earlier this session), all 5 NBA-relevant SQLAlchemy script paths in `etl/` are Mac-ready when `AZURE_SQL_TRUST_CERT=yes` is set. The only remaining holdouts are the 5 raw `pyodbc.connect` scripts that use `;`-separated DSN syntax (`etl/lineup_cleanup.py`, `etl/cleanup_stale_odds_and_grades.py`, `etl/gate_check.py`, `etl/game_day_gate.py`, `etl/seed_user_codes.py`); out of scope for this commit.

Production validation on Azure SQL: `compute-patterns.yml` dispatch run `24954477255` (VM runner, default-`no` path, post-`5e5d5df`) succeeded in 5m 41s and produced the same line counts the workflow has been producing on its nightly schedule: 30,675 patterns computed at `min_games=10`, 3,789 strong patterns (>=20pp lift), 30,675 rows upserted to `common.player_line_patterns`. No regression.

`cf89f5b` added `.github/workflows/compute-patterns-mac.yml` as a write-path mirror of `compute-patterns.yml`. `runs-on: [self-hosted, mac-runner]`, manual dispatch only (no schedule trigger, so the VM nightly 07:30 UTC remains the sole scheduled writer to Azure SQL `common.player_line_patterns`), `AZURE_SQL_*` sourced via the ADR-20260426-2 alias pattern from `/Users/schnapp/sql-server.env` plus `AZURE_SQL_TRUST_CERT=yes`, and a new `min_games` workflow_dispatch input defaulting to 10. Mac-side validation: dispatch run `24954610068` succeeded in 4m 15s (faster than Azure SQL via localhost), produced 28,910 patterns and 3,775 strong patterns, and upserted 28,910 rows to the local container's `common.player_line_patterns`. The 1,765-pattern shortfall vs the VM run is expected source-data drift, not a code discrepancy: the Mac container's `common.daily_grades` is BACPAC-era (2026-04-25 ~23:40 PDT) and is missing roughly a day of newly-resolved outcomes, so fewer player-line pairs clear the `min_games >= 10` threshold. Same code, same logic, less source data. The `mssql` container peaked at ~3.99 GiB / 5.78 GiB (69%) with ~78% CPU during the run; the 4500 MB `max server memory` cap held cleanly. See ADR-20260426-2.

2026-04-26 [shared][infra][odds] First write-path workflow migrated to the Mac runner: `odds-etl-mac.yml` mirrors `odds-etl.yml` 1:1 in terms of inputs and modes (pipeline/upcoming/discover/backfill/mappings/probe) but with three deliberate differences: `runs-on: [self-hosted, mac-runner]`, no schedule trigger (manual dispatch only, so the VM remains the sole scheduled writer against The Odds API and credits aren't double-billed), and `AZURE_SQL_*` sourced via the ADR-20260426-2 alias pattern from `/Users/schnapp/sql-server.env` plus `AZURE_SQL_TRUST_CERT=yes`. `etl/odds_etl.py` runs unmodified — the env-driven trust-cert change to `etl/db.py:_build_conn_str` shipped earlier this session is what makes this workflow's `get_engine` path work against the local container without source changes. Default sport is `nba` per current sport priority. Default mode is `upcoming` rather than `pipeline` because pipeline (discover+backfill+mappings) is credit-expensive.

End-to-end validation: dispatch run `24954217369` (mode=upcoming sport=nba days_ahead=1) succeeded, fetched 4 upcoming NBA events for 2026-04-26 (Raptors-Cavs, Blazers-Spurs, 76ers-Celtics, Rockets-Lakers), wrote 1,048 rows to `odds.upcoming_game_lines` and 7,572 rows to `odds.upcoming_player_props` on the local container. Sport-scoped truncate-and-reload preserved: post-run row counts are 14 MLB events + 4 NBA events = 18, and 4,675 MLB props (untouched, `created_at = 2026-04-25 13:18 UTC` from BACPAC) + 7,572 NBA props (`created_at = 2026-04-26 10:18 UTC`, written within the run window). MLB rows were not disturbed by the NBA-scoped run, exactly matching VM behavior.

Cost note: a single NBA-day dispatch consumed 197 Odds API credits (~50 per event because the script makes a per-event call for prop markets in addition to bulk endpoints). This is normal for `upcoming sport=nba` and matches what the VM's daily scheduled run consumes. The Mac mirror is for ad-hoc validation and on-demand local-DB freshening; running it routinely would double daily credit consumption against the API. Commit `adce3a3`.

2026-04-26 [shared][etl] Migration pattern from ADR-20260426-2 extended to both SQLAlchemy engine builders. `etl/db.py:_build_conn_str` and `etl/nfl_etl.py:get_engine` (a parallel implementation that bypasses `etl.db` entirely) now read `AZURE_SQL_TRUST_CERT` with default `"no"`. Default behavior preserves Azure SQL connection strings bit-exact; verified via conn-string inspection (default reproduces prior hardcoded `&Encrypt=yes&TrustServerCertificate=no` to the character; opt-in with env=yes flips only that one parameter). `etl/nfl_etl.py:get_engine` previously omitted `Encrypt` and `TrustServerCertificate` entirely and relied on ODBC Driver 18 defaults (`Encrypt=yes`, `TrustServerCertificate=no`); both are now explicit so the env-var opt-in works on the Mac path. End-to-end validated by manually dispatching `nfl-etl.yml` on the VM: run `24953986007` completed in ~2 minutes and upserted 165,657 rows across seven NFL tables (games 285, players 24633, player_game_stats 19421, snap_counts 26612, ftn_charting 47316, rosters_weekly 46820, team_game_stats 570). Unblocks Mac migration of the seven get_engine-only workflows: `lineup-poll`, `mlb-etl`, `mlb-pbp-etl`, `nba-live`, `nfl-etl`, `odds-etl`, plus `lineup-backfill` when needed. Five "mixed-mode" scripts that use `get_engine` AND have hardcoded raw `pyodbc.connect` calls (`compute_patterns.py`, `migrate_common_teams.py`, `nba_clear.py`, `nba_etl.py`, `signal_backtest.py`) still need their raw-connection sites touched separately before their workflows can move; that work is deferred to whichever workflow gets picked next. Initiative B in ROADMAP captures the broader DRY pass that would consolidate all of these. Commit `0769e68`.

2026-04-26 [shared][infra][etl] Mac-runner pilot extended to its first production-script-equivalent workflow. Three operational changes on the Mac and one workflow migration. (1) `max server memory (MB) = 4500` set on the local SQL Server container via `sp_configure`; persists across restart in the named volume `mssql-data`; sized for Colima's 6 GiB allocation with ~1.4 GiB host headroom. (2) `KeepAlive=true` added to the mac-runner launchd plist (`~/Library/LaunchAgents/actions.runner.SchnappAPI-sports-modeling.mac-runner-1.plist`); reload via `launchctl unload && load` proven, runner picked up `mac-runner-pilot.yml` run `24953581481` post-reload. (3) New workflow `db_inventory-mac.yml` runs the production `etl/db_inventory.py` (unmodified) against the local container by sourcing `/Users/schnapp/sql-server.env` and re-exporting `MSSQL_SA_PASSWORD` as `AZURE_SQL_PASSWORD` along with the other `AZURE_SQL_*` literals plus `AZURE_SQL_TRUST_CERT=yes`. The script's `TrustServerCertificate` setting was made env-driven (default `"no"` preserves Azure SQL behavior bit-exact); this is the only source-code change. Side-by-side runs `24953793617` (VM, Azure SQL) and `24953795344` (Mac, local container) completed in 29 s and 26 s respectively with full parity: archives both present, `player_props` count 8,747,393 exact match, MLB and NBA coverage windows and game counts identical, expected drift of 113 rows on `odds.upcoming_player_props` and 58 rows on `common.daily_grades` matching the live-drifting tables list. Pre-existing latent bug in `etl/db_inventory.py` surfaced and fixed in the same session: third query SELECTed unqualified `sport_key` from a `player_props pp JOIN event_game_map egm` where both tables now carry that column, failing identically on both runners with SQL Server error 209; fix is one character (`pp.` prefix) and is independent of the migration. Migration pattern documented in ADR-20260426-2; commits `1dcc9a0` and `4229379`. The VM remains authoritative for every scheduled and write-path workflow.

2026-04-26 [shared][web] Added a "Home" link to the top-right of the admin page (post-login view only) so admins on a phone PWA can return to the sport picker without using the browser back button or re-typing the URL. `web/app/admin/page.tsx` wraps the existing `<h1>Admin</h1>` in a `flex justify-between` row with a small bordered anchor matching the existing "Refresh" button styling. Login screen layout unchanged.

2026-04-26 [shared][infra][web] Resolved a 14-hour SWA deploy outage and removed its root cause. Diagnosis: `maintenance_mode` was toggled on at 2026-04-25 18:36:30 UTC mid-deploy of an unrelated schema commit; middleware returned HTTP 503 to anonymous traffic, which Azure SWA's anonymous warmup probes treated as an unhealthy revision and timed out after ~10 minutes. 13 consecutive deploys failed with "Web app warm up timed out" (commits `e87c70b` through `584c5b7`), so all changes pushed in that window — `/transparency` daily granularity, the calibrator Path-1 read, the Mac-runner pilot doc commits, the home-page sport picker rewrite, the admin maintenance banner and Saved toast, and the always-show admin link — never shipped. The operator did not notice because the `sb_unlock` cookie bypasses the maintenance gate, masking the live state of the site for any signed-in admin. Resolution: flipped `common.feature_flags.maintenance_mode` to `0` directly in Azure SQL, then pushed an empty commit (`0be6cdf`) which deployed cleanly in ~3 minutes and brought all 13 commits' worth of code live in one revision. Preventative fix: `web/middleware.ts` now returns HTTP 200 with the maintenance HTML body instead of 503 with `Retry-After: 3600`. The maintenance HTML carries `noindex,nofollow` so 200 does not cause SEO issues; in practice nothing on this site honored the Retry-After signal anyway (PWA service worker does not cache 503 navigations, Uptime Robot is paused, crawlers are excluded by meta tag). Operators can now toggle maintenance freely without bricking the next deploy. See ADR-20260426-1, web/middleware.ts.

2026-04-26 [shared][web] Always show the home-page admin link; dropped the `sb_unlock` cookie gating that was added in `27fd10f`. `web/app/page.tsx` is no longer a server component (no cookie read), and `web/app/HomeHub.tsx` no longer takes a `showAdminLink` prop. The link is unconditionally rendered in the top-right of `/`. The link is small and low-contrast and `/admin` itself is passcode-gated, so visibility does not expose anything. The earlier conditional gating created a bootstrap problem on a fresh device with one PWA installed at `/`: no cookie meant no link, leaving the hidden triple-tap `AdminTrigger` as the only path to admin. The `sb_unlock` cookie itself is unchanged: still set by authed admin calls and still consulted by `web/lib/feature-flags.ts` for the admin bypass on flag gating and by `/transparency` access. Only the home-page link visibility check was removed. Triple-tap `AdminTrigger` in `web/app/layout.tsx` is unchanged. Commit `18c196b`.

2026-04-26 [shared][web] Home page is now the sport picker. `web/app/page.tsx` is a server component that reads the `sb_unlock` cookie and renders new client component `web/app/HomeHub.tsx` (the picker UI previously at `/lol`) with a `showAdminLink` prop. When the admin cookie is present, a small `admin` link appears in the top-right of the home page; anonymous visitors see no admin affordance there. The triple-tap `AdminTrigger` in `web/app/layout.tsx` is unchanged and remains the universal escape hatch on every page. `/lol` now redirects to `/` for backward compatibility.

Admin page UX fixes (`web/app/admin/page.tsx`): (1) yellow banner under the h1 when `maintenance_mode` flag is enabled, with a one-line note that the admin viewer is bypassed by the `sb_unlock` cookie and that an incognito window is required to verify the gate is working — addresses the "I flipped maintenance on but the site looks normal" confusion which is the cookie bypass behaving as designed, not a bug. (2) Brief "Saved" toast at the bottom of the admin viewport for ~1.5 s after a successful flag toggle, "Save failed" toast for ~2.5 s on error — addresses the "no way to save" perception by making the existing immediate-PATCH-on-toggle behavior visible. No changes to the underlying `/api/admin/flags` endpoint or the `common.feature_flags` schema.

Commit: `27fd10f`. Auto-deploys via Azure SWA workflow on push to main.

2026-04-26 [shared][infra] Mac runner pilot live. Second self-hosted GitHub Actions runner registered on Schnapps-MBP alongside the existing Azure VM runner. Name `mac-runner-1`, label `mac-runner`, version 2.334.0, installed at `/Users/schnapp/actions-runner/`, launchd user agent at `~/Library/LaunchAgents/actions.runner.SchnappAPI-sports-modeling.mac-runner-1.plist` with `RunAtLoad` set (no `KeepAlive` yet, follow-up if pilot proceeds). Production workflows continue to target `schnapp-runner` and are unaffected.

Dependencies installed on the Mac to mirror the VM ETL toolchain: ODBC Driver 18.6.2.1 + unixODBC 2.3.14 + mssql-tools18 via Microsoft's `microsoft/mssql-release` Homebrew tap; Python 3.12.13 venv at `/Users/schnapp/venv` with the full `etl/requirements.txt` (sqlalchemy 2.0.49, pyodbc 5.3.0, pandas 3.0.2, numpy 2.4.4, requests 2.33.1, nba_api 1.11.4, mlb-statsapi 1.7.2).

New pilot workflow `mac-runner-pilot.yml` runs `etl/local_db_inventory.py` against the local SQL Server 2022 container (Docker on Colima, set up in the prior session, populated by a 14.8M-row BACPAC import of `sports-modeling` with row-count parity verified vs Azure SQL). The pilot script is read-only: queries `@@VERSION`, table counts per schema, and row counts for `common.daily_grades`, `odds.player_props`, `mlb.play_by_play`. Manual dispatch only. First run (24951996301) succeeded in 22 seconds, proving the full chain: GitHub triggers, mac-runner picks up the job, `actions/checkout@v4` clones, bash sources `/Users/schnapp/sql-server.env` with secret masking via `::add-mask::`, the Python venv activates, pyodbc connects via ODBC Driver 18 to `localhost,1433`, queries return, exit 0.

This is a pilot only. No production workflow has been migrated. The VM runner remains authoritative for every scheduled and dispatch-only workflow currently in `.github/workflows/`. Next concrete step if the Mac path proceeds is migrating one production workflow (likely `db_inventory.yml` first, then a write-path workflow) and validating parity before any cutover. Commits: `70e544c` (pilot workflow + script).

2026-04-25 [nba][grading] Schema and code prep for the immutable-corpus architecture (ADR-20260425-3 follow-on). Three commits: `e87c70b` adds MODEL_VERSION stamping; `a8b1068` adds weekly calibration job and workflow.

Schema changes (idempotent ALTER ADDs, executed live before code push):
- common.daily_grades.model_version VARCHAR(50) NULL
- common.daily_grades_archive.model_version VARCHAR(50) NULL
- common.player_tier_lines.model_version VARCHAR(50) NULL
- New table common.grade_calibration_history(snapshot_id, snapshot_date, sport, bucket_min, bucket_max, sample_size, empirical_hit_rate, isotonic_hit_rate, max_well_sampled_rate, window_days, model_version, created_at) with index on (sport, snapshot_date) for the /transparency trend chart.

grade_props.py changes:
- New constant MODEL_VERSION = "adr-20260425-3"
- ensure_tables now idempotently adds model_version columns to all three tables (so future provisioning works even if the live ALTER step is skipped).
- upsert_grades and upsert_tier_lines stage tables, INSERT placeholders, and MERGE clauses all extended to carry model_version through.
- _ARCHIVE_COLS extended so the archive trigger preserves the version stamp.
- Every row written from this commit forward carries its model version stamp; old rows have NULL (treated as "unknown").

New: grading/weekly_calibration.py and .github/workflows/weekly-calibration.yml. Cron Sunday 06:00 UTC, manual dispatch supported with --dry-run. Reads a 30-day rolling window of resolved tier-line outcomes, fits the calibrator with PAV isotonic regression and an n>=30 well-sampled threshold for the safety cap, then writes one snapshot row per bucket to common.grade_calibration_history AND replaces common.grade_calibration so the daily grading run picks up the fresh calibrator. Threshold choice (30 vs 50/100/adaptive) was validated against backtest data: fixed-30 produces a stable cap across all rolling windows from Nov-Apr; adaptive (max(20, 0.5%)) varies more wildly because in big-data months it pushes the threshold to 100-200 and kicks out legitimate buckets; n=10 fails once with a fluke 1.0 cap.

Architecture deferred (waiting on in-flight backfill chain to drain naturally, currently 109/143 dates):
- Stop chain redispatch in grading.yml
- Run weekly-calibration.yml manually once to populate grade_calibration_history with the first snapshot
- Update grade_props.py to read calibrator from common.grade_calibration instead of refitting walk-forward each grade run

When the chain ends, those three steps ship the architectural transition cleanly: no more regrading, every model version's accuracy preserved in the immutable corpus, weekly recalibration becomes the only writer of common.grade_calibration. See ADR-20260425-3.

2026-04-25 [shared][web] /transparency page live behind `page.transparency` flag (seeded disabled). Read-only model accuracy log accessible to admins via the `sb_unlock=go` cookie (signing into `/admin` grants it). Three sections: (1) per-tier predicted vs actual hit rates with last-30/last-90/all-time window toggle, (2) weekly trend chart (inline SVG, no external lib), (3) latest calibration bucket snapshot from `common.grade_calibration` with the global output cap surfaced (`max_well_sampled_rate`, ADR-20260425-3). Three new API routes `/api/calibration-buckets`, `/api/tier-accuracy?window=...`, `/api/tier-accuracy-trend`, all `force-dynamic`. Admin Visibility tab now lists `page.transparency` with the label "Transparency". Pure additive change; no existing surfaces touched. Page works today off the live tables. When the weekly-calibration job lands, the trend view will pull from `common.grade_calibration_history` for snapshot-quality data instead of recomputing from `player_tier_lines + daily_grades` on each load. Commit `f5d97a6`. See ADR-20260425-3.

2026-04-25 [nba][grading] Two-cap fix landed for Safe-tier overconfidence (ADR-20260425-3). Backtest on 88,922 to 103,365 resolved tier-line rows from the in-flight ADR-6 backfill identified two stacking failure modes in the high-prob tail: (1) thin-history rows (`n_games < 10`, the `_kde_prob_above` normal-dist fallback) hit 69.5% on prob>=0.80 vs 85.4% predicted (-15.9 pts gap), driven by microscopic std on 3-5-sample windows producing 99.99% probabilities; (2) PAV pooling in `calibrate_grades.fit_calibrator` forced the small-sample 0.90+ tail (n=135 combined) UP to 84.7% to match the much larger 0.85-0.90 well-sampled bucket (n=1814, empirical 82.3%), even though those tail buckets had empirical 65-78%. Two surgical caps:

1. `grade_props.py` — added `KDE_THIN_SAMPLE_PROB_CAP = 0.85`. Both fallback branches in `_kde_prob_above` (the `n < KDE_MIN_GAMES` path AND the `except Exception` after KDE fitting fails) now return `min(prob, KDE_THIN_SAMPLE_PROB_CAP)`. Caps the worst overconfidence cases without touching the n>=10 majority.

2. `grading/calibrate_grades.py` — `fit_calibrator` computes `max_well_sampled_rate = max(stats.hit_rate where n >= 200)` at fit time (currently ~0.8225). Inside the `calibrator` closure, the standard PAV-interpolated value is then clamped: `return min(v, max_well_sampled_rate)`. `publish_calibration_buckets` extended to denormalize the cap onto every row of `common.grade_calibration` via a new column `max_well_sampled_rate FLOAT NULL` (idempotently added).

Backtest simulation: Safe overall gap shrinks from -10.0 pts to -8.7 pts; Safe high-prob (>=0.80) gap shrinks from -10.2 pts to -7.8 pts. Zero rows lose Safe qualification because the cap (~0.82) is above the 0.80 threshold. Realized return on Safe ($-0.04 per dollar) is unchanged - the same rows still bet, the model just stops misrepresenting their probability. Modest improvement, not a fix; the deeper structural problem (Safe at -500 implied breakeven 83.3% vs model delivering ~75% on the well-sampled tail) is not addressed and is deferred. The in-flight backfill chain (run 24933461522) picks up the changes on next iter; in-flight Python processes do not. Web transparency page surfacing per-bucket calibration accuracy is the follow-on deliverable. See grading/grade_props.py, grading/calibrate_grades.py, docs/DECISIONS.md ADR-20260425-3, /tmp/sim_v2.py.

2026-04-25 [shared][web] Visibility framework live. New `common.feature_flags` table (PK `flag_key VARCHAR(100)`, `enabled BIT`) seeded with `maintenance_mode`, `sport.nba`, `sport.mlb`, `sport.nfl`, `page.nba.grades`, `page.nba.player`, `page.mlb.main`. Replaces the hardcoded `MAINTENANCE_ON` constant in `web/middleware.ts`. New `web/lib/feature-flags.ts` exports `isPageVisible(flagKey)` for server components: 60-second in-process cache, admin-cookie bypass via `sb_unlock=go`, cascade so any `page.<sport>.<x>` lookup short-circuits to false when its parent `sport.<sport>` is disabled, fail-open on DB error. New `web/components/ComingSoon.tsx` placeholder. Sport gates added to `web/app/nba/page.tsx`, `web/app/mlb/page.tsx`, and the new `web/app/nfl/page.tsx`; sub-page gates on `web/app/nba/grades/page.tsx` (`page.nba.grades`) and `web/app/nba/player/[playerId]/page.tsx` (`page.nba.player`). Admin UI (`web/app/admin/page.tsx`) now has three tabs: Codes (existing), Visibility (toggles every flag), Tools (Refresh Data button using the admin session header, no separate `ADMIN_REFRESH_CODE` prompt). Triple-tap on the top-left 32x32 corner of any page navigates to `/admin`. Admin login auto-grants `sb_unlock`, so signing into `/admin` unlocks the maintenance gate without a separate `?unlock=go` step. Inline `RefreshDataButton` removed from `web/app/nba/NbaPageInner.tsx` and `web/app/nba/grades/GradesPageInner.tsx`; the unused `handleRefreshComplete` function in `GradesPageInner.tsx` was deleted to satisfy the lint pass. Three commits landed: `7c9d52f` (DB-backed flags + admin auth on refresh), `4f5da59` (admin tabs + triple-tap trigger), `7e65752` (visibility gates on sports + sub-pages, NFL placeholder, drop inline refresh). Live and SWA-deployed. See ADR-20260425-2 for the design rationale.

2026-04-25 [shared][web] Maintenance gate ON. Flipped `MAINTENANCE_ON = true` in `web/middleware.ts`. Site-wide 503 maintenance page for everyone except visitors with the `sb_unlock` cookie. Operator unlock: `https://schnapp.bet/?unlock=go`. `/api/ping` still open. To disable, flip back to `false` and push.

2026-04-25 [shared][web] Simplified maintenance gate from env-var-driven to two hardcoded constants in `web/middleware.ts`: `MAINTENANCE_ON` (bool) and `UNLOCK_CODE` (string). Removes Azure SWA env var dependency, fail-open misconfiguration logic, and the need to remember which portal panel to flip. Toggle is now: edit the constant, commit, push, wait ~90s for SWA redeploy. Cookie behavior unchanged (`sb_unlock`, 30 days, HttpOnly). Removed `MAINTENANCE_MODE` and `MAINTENANCE_BYPASS_CODE` references from /docs/CONNECTIONS.md and /web/README.md. See /web/middleware.ts.

2026-04-25 [shared][web] Root landing changed from `/nba` to `/lol`. `web/app/page.tsx` now redirects to the sport-picker hub at `/lol` instead of jumping straight to the NBA page. One-line change. See /web/README.md.

2026-04-25 [shared][web][infra] Added site-wide maintenance gate at `web/middleware.ts`. When `MAINTENANCE_MODE=1` is set in Azure SWA app settings, every request returns a generic 503 maintenance page except `/api/ping` (DB keep-alive). Operator bypass: visit any URL with `?unlock=<MAINTENANCE_BYPASS_CODE>`; middleware sets HttpOnly+Secure+SameSite=Lax cookie `sb_unlock` for 30 days, then 307-redirects to the same URL stripped of the unlock param so the code never sticks in the address bar. Cookie value equals the bypass code so guessing the cookie name does not bypass; you must know the code. Fails open if either env var is unset (typo safety). Runs before the existing `common.user_codes` passcode layer; passcode users still see the maintenance page when the gate is engaged. Toggle on/off via Azure SWA env vars without redeploy. New env vars: `MAINTENANCE_MODE`, `MAINTENANCE_BYPASS_CODE`. See /web/README.md, /docs/CONNECTIONS.md Azure Static Web Apps app settings.

2026-04-25 [nba][grading][analysis] Backfill chain (run 24933461522) at 72/143 ADR-6 dates after ~64 min wall time, slower than handoff projection. Iter 1 wrote 71 dates from Oct 27 forward; iter 2 (PID 327558) currently mid-flight, committing per-date but at slower rate as walk-forward calibration history deepens (each `fit_calibrator(as_of_date=...)` call processes more resolved samples on later dates). Backtest re-run on the 72-date dataset (88,922 resolved tier-line rows via `/tmp/backtest3.py`) shows the Safe undershoot is structural, not a thin-history artifact: Safe 64.7% actual vs 80% design (-15.3 pts gap), worse than the 25-date first-pass 60.5% but the same direction. Predicted 74.8%, actual 64.7%, real return -16.75 cents per dollar. Calibration curve shows overconfidence in high-prob buckets: 0.70-0.80 predicts 73.6% / hits 62.9% (-10.7 pts), 0.80-0.90 predicts 83.5% / hits 74.3% (-9.2 pts), 0.90-1.01 predicts 95.0% / hits 65.8% (-29.3 pts on n=114). Value remains dead-on at 61.1% actual vs 58% design (+1.4 pts) but real return -9.6 cents from vig. HighRisk 22.7% actual vs 28% design (-5.3 pts), real -14.8 cents. Lotto 9.1% actual vs 7% design (+2.1 pts on hit rate) but predicted 12.7% so calibration overshoots, real -27.5 cents. ADR-6 OR-gate cohort split: HighRisk Breakout-only n=303 hits 16.8% / real -6.0 cents (longer prices than Standard which loses -12.2 cents on n=4,675); Lotto Breakout-only n=386 hits 3.6% / real -61.6 cents (catastrophically -EV at +400 prices missing 96% of the time). ADR-6 hit-context columns show minimal signal: HighRisk minutes-match 23.2% vs minutes-below 22.4% (delta 0.8 pts); Lotto minutes-match 8.9% vs minutes-below 10.6% (counterintuitive). Safe EV buckets are anti-predictive at the top: rows with predicted EV +50% or higher (n=216) hit 43.1%, lower than the +0% to +10% bucket which hits 61.9%. The model's high-confidence picks systematically miss. Investigation of high-prob calibration undershoot is the next active work. Held topics still held: walk-forward calibration mechanism (working as designed), 4 EV columns (working as designed), composite reweighting (deferred). See grading/grade_props.py, /tmp/backtest3.py, /tmp/check_backfill.py, docs/DECISIONS.md ADR-20260424-6.

2026-04-25 [infra][security] Replaced failing `github@claude-plugins-official` Claude Code plugin (Copilot-auth, requires Copilot subscription) with a PAT-based GitHub MCP server. Plugin disabled (not uninstalled). New stdio MCP `github` registered at user scope in `~/.claude.json` on the Windows laptop, running `npx -y @modelcontextprotocol/server-github` with `GITHUB_PERSONAL_ACCESS_TOKEN` env var. Token: fine-grained PAT named `Claude Code MCP - sports-modeling`, all SchnappAPI repos, Actions/Contents/Issues/Pull requests/Workflows read+write + Metadata read, expires 2027-04-25. Local-only credential — does not need to rotate alongside `GITHUB_PAT`/`GH_PAT`. Mobile (claude.ai) continues to use the separate `Claude MCP` classic PAT via the GitHub connector. See /docs/CONNECTIONS.md Personal Access Tokens.

2026-04-25 [shared][docs] Added live-session-cache skill: per-turn chat logging on dedicated `chat/*` branches separate from the work branch. Skill lives at user level (`~/.claude/skills/live-session-cache/SKILL.md`, generic across projects) with project-specific integration at `/docs/skills/live-session-cache.md`. Three modes: `trigger` (default on claude.ai chat, activates on first non-chats repo write and backfills the conversation as Turn 0), `always` (default on Claude Code, activates at session start), `manual`. Each turn block captures User verbatim, Reasoning summary (2 to 5 sentences), Response verbatim, optional Evolution Note for framing shifts that did not produce code, and State Delta with work-branch commit hash references. Chat files at `chats/in-progress/{slug}.md` move to `chats/archive/{YYYY}/{MM}/{slug}.md` on wrap-and-merge. Branch convention `chat/YYYY-MM-DD-{slug}`. Wrap opens a PR titled `chat: {slug}` with squash-merge recommended in the PR body so main history stays clean. Context monitoring fires soft warning around 60-70% of capacity (one-time note) and hard warning around 85% (presents wrap, pause, or keep-going options). Chat log is supplementary to existing end-of-session protocol; CHANGELOG, ADR, INVARIANTS edits all still required. Alternatives rejected: separate `claude-chats` repo (loses code-to-chat proximity), always-on across both surfaces (claude.ai per-turn full-file upload too expensive for short chats), heuristic substantive-content detection (judgment risk; binary first-non-chats-write is mechanical and matches user's actual definition). docs/skills/session-protocol.md updated with a new "Live session cache" section before "When to deviate"; docs/README.md updated to list the new skill doc under "Read first every session." Rolled out via 5 GitHub MCP commits to main directly because Claude Code session was unavailable mid-flow. See /docs/skills/live-session-cache.md, /docs/DECISIONS.md ADR-20260425-1.

2026-04-24 [nba][web] Added player team tricode badge to At a Glance list view (GradesPageInner.tsx) and PropMatrix view (PropMatrix.tsx). Player team is derived inline from existing row data: oppTeamAbbr === homeTeamAbbr ? awayTeamAbbr : homeTeamAbbr. Rendered as a small muted text-[10px] gray-500 chip immediately before the player name, sharing the existing flex-wrap container with W/L badges and signal chips. No schema or API change. See /web/components/README.md and /web/app/nba/grades/README.md.

2026-04-24 [nba][web] PropMatrix player sort changed from alphabetical to right-to-left lexicographic over the threshold ladder, mirroring FanDuel's Live SGP grid. For each stat group (PTS/REB/AST/3PM/PRA/etc.) within a game, players are now ordered by walking the columns from highest threshold leftward: a populated cell ranks above a missing one; if both are populated, sort ASC by American odds (more negative = more favored = higher); if both are equal or both missing, walk one column left and repeat. Alphabetical is the final tiebreaker. One comparator change at PropMatrix.tsx line 454, no schema or API change. See /web/components/README.md.

2026-04-24 [nba][web] Fixed Props tab HTTP 500 caused by /api/grades timing out at the 15s SWA limit. Root cause: getGrades in web/lib/queries.ts wrapped the date column in CONVERT(VARCHAR(10), dg.grade_date, 120) = @gradeDate, which is non-sargable and forced a scan instead of an index seek. With the walk-forward backfill loading common.daily_grades, the query crossed the 15s threshold and the API returned 500. Reproduced against DB: 52.5s with the CONVERT predicate, 0.12s with sargable dg.grade_date = @gradeDate (column is DATE, param is YYYY-MM-DD varchar; SQL Server casts the constant once). Two other CONVERT calls in SELECT clauses at lines 519 and 594 are output formatting, kept as-is. The same code path feeds At a Glance and the player Props page, both should also be faster. See /web/nba/README.md.

## 2026-04-24 [nba][grading][schema][adr] ADR-20260424-6 — tier qualification redesign

Refines ADR-5. Three changes addressing direct user feedback that the strict
probability gate hides breakout candidates and that Safe rows can be -EV at
the boundary.

**Safe tier:** add EV floor `TIER_SAFE_EV_FLOOR = -0.05`. Drops Safe rows
where calibrated per-dollar EV is worse than -5 cents. -500 implied-odds
ceiling unchanged.

**HighRisk and Lotto:** OR-gate qualification. Surface row if calibrated
probability clears tier threshold OR breakout_signal fires. breakout_signal =
(`recent_opportunity / historical_opportunity >= 1.15`) AND
(`recent_minutes_20 >= season_avg_minutes * 0.95`). Composite >= 50 outer
gate for Lotto also relaxed by breakout_signal. Captures the explicit "player
has never hit X but is trending up" case Austin asked us to find.

**4 hit-context columns** on `common.player_tier_lines`:
`highrisk_hit_avg_min`, `highrisk_hit_avg_opp`, `lotto_hit_avg_min`,
`lotto_hit_avg_opp`. Avg minutes / market-relevant opportunity in past games
where stat met or exceeded the rare-tier line. Lets the user compare today's
projected role to the conditions that produced past hits without joining
back to nba.player_box_score_stats. Computed from an aligned (player_id,
game_date) merge of stat_grp and opp_grp; populated only when hits > 0 and
the relevant column is available (combo markets PR/PA/RA/PRA have no
opportunity mapping per ADR-5 design, so their opp variant remains NULL).

Schema: `common.player_tier_lines` now 49 physical columns (47 in upsert
ALL_COLS + audit). Migration via `migrate-tier-lines-v4.yml`, idempotent
ALTER ADD IF NOT EXISTS. `ensure_tables` in grade_props.py also handles the
4 new cols safely so any future fresh DB applies the same schema.

Walk-forward backfill restarted at run 24919618493 with mode=backfill,
force=true, batch=30, time_limit=300. Expected 5-15% net change in tier row
volume per day: EV floor drops some -EV Safe rows; OR-gate admits breakout
HighRisk/Lotto rows. First 4 dates (Oct 27-30) confirm the new schema
populates correctly: 100% of HR/Lotto rows with hits>0 populate
hit_avg_min; ~55-65% populate hit_avg_opp (combo markets are NULL by
design, single-stat markets populate).

Audit note: this ADR exists because the prior session's commit
`f51d61e` added walk-forward + EV columns based on AI inference rather
than verbatim user instruction. ADR-6 brings the qualification logic
back in line with Austin's stated tier philosophy: identify breakout
candidates, do not hide rows where the model probability is low but
the trend points to upside.

Files: docs/DECISIONS.md (ADR-20260424-6), grading/grade_props.py
(constants + compute_kde_tier_lines + ensure_tables + upsert_tier_lines +
grade_props_for_date caller), .github/workflows/migrate-tier-lines-v4.yml
(new).

Append-only. Newest at top. One entry per session-end.

Format:

```
YYYY-MM-DD [scope][component] One-line summary. See /path/README.md.
```

Tag taxonomy is defined in `/docs/README.md`. Filter entries by relevant tags rather than reading top-to-bottom.

Historical entries from before the documentation restructure are preserved at `/docs/_archive/CHANGELOG.md`. That file is no longer appended to; new work lands here.

---

2026-04-24 [nba][grading][docs] Aligned ADR-4 step 4 and ADR-5 step 6 to the full-season walk-forward backfill plan; deleted rebuild-tier-lines-30d.yml. Both ADRs previously said "Force-regrade last 30 days" which contradicted Austin's stated position that historical regrades must cover every date with odds (walk-forward calibration via as_of_date is what makes early-season correlation analysis meaningful; partial regrades mix two model versions). The 30-day language was the source of the rebuild-tier-lines-30d.yml workflow that ran at 22:21 UTC and deleted player_tier_lines for Mar 25 - Apr 24. The deletion is self-healing because the running full-season backfill (run 24916762938) overwrites every date including those 30 as it walks oldest-first. Workflow file deleted to remove the tripwire. ADR amendments are inline notes dated today, append-only per file convention. Walk-forward calibration (commit f51d61e) and EV columns (safe_ev/value_ev/highrisk_ev/lotto_ev, same commit) match the resumption primer parts 1 and 2 verbatim — those are not drift, they are the plan. Drift was confined to: (a) the deleted 30-day cleanup workflow, (b) the now-corrected ADR step language. Full-season backfill is the active operation; ETA tonight.

2026-04-24 [nba][grading][infra] Restored full-season walk-forward backfill chain; redispatch step rewritten to not depend on `gh`. Today (2026-04-24) NBA Grading run 24914508464 completed batch 1 of the --force --batch 30 backfill (Oct 21 -> ~Nov 2, 30 dates, walk-forward calibration via fit_calibrator(as_of_date=grade_date_str) confirmed active). The redispatch job then exited code 127: `gh: command not found` on the self-hosted runner, breaking the auto-chain after one batch. Diagnosis: `common.player_tier_lines` had max grade_date 2026-03-24 with 153,594 rows, of which only 2,132 had safe_ev populated, all on 2025-10-27 through 2025-11-02 (the only batch processed). User-visible site for last 30 days (Mar 25 - Apr 24) was empty because rebuild-tier-lines-30d.yml (run 24914479404 at 22:21 UTC) had deleted those rows as a one-shot cleanup before the regrade started. Fix: rewrote redispatch step in .github/workflows/grading.yml (commit 2873446) to call the /actions/workflows/grading.yml/dispatches REST endpoint directly via curl, with `permissions: actions: write` and `contents: read` set at job level. Removes the dependency on `gh` entirely. Then dispatched grading.yml in upcoming mode (run 24916058656) to repopulate today's tier_lines (229 rows written), and re-dispatched grading.yml in backfill mode with force=true batch=30 time_limit_minutes=300 (run 24916762938) to resume the walk-forward chain at batch 2. Chain will now self-perpetuate via the curl-based redispatch. Remaining gap: dates 2026-03-25 through 2026-04-23 still have zero tier_lines until backfill walks oldest-first to that window (~4-5 batches at ~70 min/batch, expected complete tonight). The naming `rebuild-tier-lines-30d.yml` was misleading: it is a one-shot cleanup of the most-recent 30 days of stale tier rows, not the scope of the regrade. The regrade itself is full-season (Oct 21 - present) per --force on run_backfill.

2026-04-24 [shared][grading][schema] ADR-20260424-5 implemented and smoke-tested. Schema migration ran via migrate-tier-lines-v3.yml (run 24913850519), adding 21 additive columns to common.player_tier_lines (41 total): 16 per-tier hit evidence (hits_all, games_all, hits_20, games_20 for safe/value/highrisk/lotto), 2 tier prices (safe_price, value_price), 3 opportunity context (recent_minutes_20, recent_opportunity, historical_opportunity). New module grading/calibrate_grades.py (167 lines) fits isotonic regression on resolved tier-line outcomes via pool-adjacent-violators, no sklearn dep, publishes inspection buckets to common.grade_calibration, identity fallback when <100 resolved samples. grade_props.py edits: added TIER_EXCLUDED_MARKETS (blocks + steals), IMPLIED_ODDS_CEILING=-500, RECENT_GAMES_WINDOW=20, MARKET_OPPORTUNITY_COL map (fga for points markets, fg3a for threes, reb_chances for rebounds, potential_ast for assists; combos NULL in v1). Extended fetch_opportunity_history to SELECT minutes. Rewrote compute_kde_tier_lines per ADR-5 controls: removed dense_grid (posted alt lines only), applied -500 implied-odds cap across all tiers, kept existing per-tier price floors (+150 highrisk, +400 lotto, composite >= 50 lotto gate), computes per-tier hit stats inline from stat_values (no extra queries), applies calibrator to every tier_prob, returns 21 new fields. Rewrote upsert_tier_lines with ALL_COLS list driving both CREATE TABLE #stage_tiers and the MERGE; stages/upserts 38 columns. grade_props_for_date now initializes _tier_calibrator once per run via fit_calibrator(engine) and pre-groups opp_df into _opp_groups for O(1) per-(player, market) lookup. Emit skipped when all four tiers NULL. Smoke test via grading.yml upcoming mode (run 24914385748) succeeded: 1603 grade rows, 264 tier_lines rows, no errors, no exceptions. Opportunity coverage: reb_chances 2755/3108, potential_ast 3070/3108. Outstanding from ADR-5 plan: force-regrade last 30 days via grading.yml backfill mode with force=true, then inspect grade_calibration buckets and new column values to validate calibration and opportunity signals.

2026-04-24 [shared][docs][grading] ADR-20260424-5 drafted — tier-line justification via historical + recent hit signals. Austin's answer to ADR-4 Q1 revealed the proximity band frame was wrong: HighRisk and Lotto tiers exist to surface extremes, not suppress them. Replaces control 3 with a historical-justification filter (at least one past hit at or above the threshold) and adds 16 new columns to common.player_tier_lines (hits_all/games_all/hits_20/games_20 per tier) so the UI can render the raw evidence alongside the calibrated probability. Market exclusions locked in: player_blocks / player_blocks_alternate / player_steals / player_steals_alternate bypass tier computation. Controls 1/2/4 of ADR-4 unchanged (posted-line source, -500 implied-odds ceiling, isotonic calibration). Context-similarity feature matching (looking at features of past games where player hit the line and comparing to tonight's context) is out of scope for v1; deferred to post-implementation evaluation. ROADMAP updated: active = implement ADR-5 (schema migration + calibrate_grades.py + compute_kde_tier_lines rewrite + 30-day backfill).

2026-04-24 [shared][etl][database] Initiative A Phase 3 progress: dry-run retroactive scan, catalog reconciled against real data, MLB odds daily cadence restored, tier-line ADR drafted. Revised plan (flip original phases 3 and 5): ran retroactive_scan(dry_run=True) before wiring validate_and_filter into any ETL. First run surfaced 272,703 violations across 8 tables — most were catalog bugs, not data bugs, confirming the revised-plan ordering was correct. Investigations via two one-off workflows (investigate-integrity-findings.yml dispatched twice, classified each dirty table): (1) odds.player_props / upcoming_player_props 234,007 NULL outcome_point traced to seven binary yes/no markets (batter_first_home_run, player_first_basket, player_double_double, player_triple_double, player_anytime_td, player_1st_td, player_last_td) that legitimately have no line value. (2) common.daily_grades 13,664 NULL hit_rate_60/grade traced 100% to sample_size_60 = 0 rows (rookies, returnees). (3) common.player_tier_lines 3,087 NULL safe_line rows are high-composite players where no posted alt satisfies the safe threshold — correct behavior surfacing as a catalog bug. (4) odds.event_game_map 3,310 NULL game_id: 2,669 NBA + 641 MLB, mostly historical 2023-2025, 15 rows from 2026. (5) odds.player_map 810 no_match rows are legitimate Layer-2 sentinel entries, not broken data. Five catalog fixes applied (feat/integrity-catalog-fixes merged as f45e9e6): outcome_point moved to required_when market_key NOT IN binary set via new _BINARY_PLAYER_MARKETS module constant; player_map player_id/matched_name moved to required_when match_method <> 'no_match'; daily_grades hit_rate_60/grade moved to required_when sample_size_60 > 0; player_tier_lines safe_line/safe_prob dropped from always_required; event_game_map game_id dropped from always_required. Post-fix dry-run: 272,703 → 29 violations across 2 tables (nba.schedule 28 All-Star edges + odds.upcoming_events 1 non-team-sport row). Validator and retroactive-scan workflows both green. Separately fixed odds-etl.yml scheduled cron to default --sport all instead of --sport nba (commit d8d0f00); MLB upcoming odds now ingest daily at 10:00 UTC like NBA. Confirmed via code inspection that grade_props.py is NBA-hardcoded (STANDARD_MARKETS, ALTERNATE_MARKETS, sport_key='basketball_nba' in 5+ SQL clauses) — MLB grading is an unbuilt feature, not a missing workflow. ADR-20260424-4 drafted for tier-line discretion redesign covering posted-line source constraint, -500 implied-odds ceiling, ±50% proximity band to posted center, and backtest-calibrated probability shrinkage. Four open questions documented in the ADR for Austin to answer before implementation. Investigation workflow investigate-integrity-findings.yml left in place for any follow-ups; retire after ADR sign-off. Next: Austin answers ADR open questions, then Phase 4 shifts to tier-line implementation (Initiative A's priority 1 per revised plan).

2026-04-24 [shared][etl][database] Initiative A Phase 2 complete: CRITICAL_FIELDS catalog + RELATIONAL_CHECKS catalog + retroactive_scan rewrite, validated against live DB. etl/integrity.py grew from 459 to 893 lines. CRITICAL_FIELDS now covers 21 tables: 8 NBA (schedule, games, teams, players, daily_lineups, player_box_score_stats, player_passing_stats, player_rebound_chances), 7 odds (event_game_map, upcoming_events, upcoming_player_props, player_props, upcoming_game_lines, game_lines, player_map), 6 common (teams, user_codes, demo_config, player_line_patterns, daily_grades, player_tier_lines). Each entry declares row_key (PK columns used in log identifiers), always_required (never-null columns), and required_when (conditional rules with both py_predicate for write-time and sql_predicate for retroactive scan). Conditional rules codified: nba.schedule.home_score/away_score required when game_status >= 2; nba.players.team_id/team_tricode required when roster_status = 1; nba.daily_lineups.position required when starter_status = 'Starter' (bench/inactive legitimately null per NBA JSON + boxscorepreviewv3 invariant); odds.{upcoming_,}game_lines.outcome_point required for spreads/totals but not h2h. Explicitly-nullable columns documented in comments so future sessions don't misinterpret them as violations: over_price on bracket lines, all secondary grade components in daily_grades, outcome pre-resolution, Value/HighRisk/Lotto tier values when no qualifying line exists. DNP clarification in nba.player_box_score_stats: row exists only for players who appeared; stat-zero is valid, stat-null is a violation; did-player-play is answered by minutes > 0 per ADR-20260424-2. RELATIONAL_CHECKS populated with 7 cross-table invariants (nba_lineup_coverage_30d, nba_boxscores_missing_30d, nba_boxscore_lineup_alignment_30d, nba_active_players_have_team, nba_games_have_schedule, odds_nba_events_mapped_24h, nba_grades_have_tier_lines_7d). retroactive_scan rewritten to use MERGE against UQ_completeness_log (idempotent, replaces prior correlated-subquery pattern that didn't actually work) and to process required_when rules via sql_predicate. New one-off workflow .github/workflows/validate-integrity-catalog.yml ran successfully (run 24910384196) after three round-trip catalog fixes: odds.player_map source_name -> odds_player_name + added sport_key, common.teams full_name -> team_name/team_tricode -> tricode/sport -> sport_key + added participant_id, common.user_codes is_demo -> mode + active. Validator dispatches with zero side effects (no writes to production or framework tables; uses SET PARSEONLY ON for RELATIONAL_CHECKS SQL syntax check). database/_shared/README.md updated with the three framework tables (ingest_quarantine, unmapped_entities, data_completeness_log) and two new invariants covering direct-write prohibition and the CRITICAL_FIELDS opt-in list. Phase 2 is opt-in: no existing ETL calls validate_and_filter yet, so this commit has zero production impact. Phase 3 next: wire validate_and_filter into NBA ETL upsert paths (nba_etl.py, odds_etl.py, grading/grade_props.py). Phase 4 builds resolve-mappings.yml + retry-incomplete.yml + daily-health-report.yml. Phase 5 runs retroactive_scan for first time and generates initial HEALTH.md. See etl/integrity.py, .github/workflows/validate-integrity-catalog.yml, database/_shared/README.md, docs/DECISIONS.md ADR-20260424-2.

2026-04-24 [shared][etl][database] Initiative A Phase 1 shipped: data integrity framework module + three new framework tables. New module etl/integrity.py (459 lines) implements CRITICAL_FIELDS and RELATIONAL_CHECKS catalog skeletons (empty — populated in Phase 2), ensure_tables() for one-call migration, validate_row() and validate_and_filter() for Layer-1 write-time enforcement, write_quarantine() for failed-row logging, retroactive_scan() for visibility-only scan over existing production rows (flags but does not move), record_unmapped_entity() for Layer-2 entity-gap logging via MERGE with idempotent UQ. Opt-in enforcement — tables not in CRITICAL_FIELDS pass through validate_and_filter() with zero DB round trips, so this commit has no behavioral effect on existing ETL. New one-shot workflow .github/workflows/bootstrap-integrity.yml ran successfully against main at 20:09 UTC (run 24909594802); three tables created in common schema: ingest_quarantine (quarantine_id BIGINT PK, table_name, row_key, row_payload NVARCHAR(MAX), failed_invariant, source_workflow, retry_count, resolved_at, resolution_notes; partial indexes on (table_name, row_key) and (last_retry_at, retry_count) WHERE resolved_at IS NULL); unmapped_entities (unmapped_id BIGINT PK, source_feed+entity_type+source_key UQ, candidate_match, candidate_method, candidate_confidence, resolved_mapping, seen_count, retry_count, resolved_at; partial index on (retry_count, last_seen_at) WHERE resolved_at IS NULL); data_completeness_log (log_id BIGINT PK, table_name+row_key+column_name UQ, first_detected_at, last_attempt_at, attempt_count, resolved_at, detected_retroactively BIT, notes; partial index on (last_attempt_at, attempt_count) WHERE resolved_at IS NULL). All three tables verified via sys.objects count check in the workflow itself. etl/_shared/README.md updated to document the new module alongside etl/db.py and cross-reference Initiative B for the remaining DRY work. Phase 2 (CRITICAL_FIELDS cataloging for NBA + common tables) is the next active work; Phase 3 wires validate_and_filter() into NBA ETL; Phase 4 builds resolver/retry/report workflows; Phase 5 runs retroactive scan and generates first docs/HEALTH.md. See etl/integrity.py, docs/DECISIONS.md ADR-20260424-2, feat/integrity-framework branch (merged as 264974c).

2026-04-24 [shared][docs] Docs audit produced 8 commits across 12 files correcting MLB/NFL status, Uptime Robot paused claims, Free trial reasoning (trial expired making auto-pause delay configurable; set to 60 min on 2026-04-24), MCP tool inventory (added shell_exec/read_file + corrected env var GITHUB_PAT->GH_PAT), Mac host status marked as planned-not-yet-set-up, CHANGELOG size reference updated to 43 KB, ADR numbering scheme reference aligned with date-based IDs per ADRs 20260422+, missing common.player_tier_lines added to shared-database docs per ADR-20260423-1, NFL database STATUS changed from "not materialized" to actual first-run counts (games 285, players 24376, player_game_stats 19421, snap_counts 26612, ftn_charting 47316, rosters_weekly 46820, team_game_stats 570), populated the empty root README.md with project overview. Three ADRs written this session: ADR-20260424-1 extends STATUS taxonomy with 'idle' and drops 'planned' from component vocabulary; ADR-20260424-2 is the full three-layer data integrity and completeness framework design (invariants at write time + quarantine table, mapping resolver for entity matching gaps, one-per-day max-3-attempts retry cadence for upstream lag cases, surfacing via docs/HEALTH.md + auto-opened GitHub Issues with data-integrity:* labels); ADR-20260424-3 sequences five streamlining initiatives A (framework) -> D (MLB player NULL fix) -> B (DRY pass) -> E (ROADMAP structure) -> C (observability). ROADMAP.md Next-up section updated with ordered initiative entries. Implementation of Initiative A is the next active work once docs/audit-2026-04-24 merges. See docs/DECISIONS.md ADR-20260424-{1,2,3}, docs/ROADMAP.md, docs/audit-2026-04-24 branch.

2026-04-24 [nba][grading][docs] Tier effectiveness evaluated against 176K actual outcomes and docs brought current to ADR-20260423-1. Backtest (script at /tmp/tier_eval.py on VM): Safe tier 85.9% actual hit rate vs 80% design (well-calibrated), Value 61.0% vs 58% (well-calibrated), High Risk 19.8% vs 28% design (overconfident by 12 points; breakeven at avg +348 is 27.8% so tier is currently -EV at its price floors; profit/$100 = -$20.56), Lotto 6.4% vs 7% design (hit rate on target but model probability of 12.6% is overconfident; profit/$100 = -$44.42 at avg +1,189). Calibration is reliable in the 50-90% probability range and degrades at both extremes (10-50% predicted overestimates actual by 10-20 points; 90-100% predicted actually hits 69.5%). Combo markets (PRA/PR/PA/RA and alternates) overperform Safe by 9-11 points; three-point markets underperform Safe by 4-6 points because KDE smooths the discrete low-count distribution too much. Blowout dampening confirmed helpful: dampened Safe rows hit 91.4% vs 88.7% not-dampened, Value 68.0% vs 62.9%. KDE window selection (15/30/82 grade-weighted) produces comparably calibrated Safe hit rates across all three windows (85.6-86.3%). Coverage 97.1% of expected rows; 2.9% missing are players below `KDE_MIN_GAMES = 10`. Brier scores: Safe 0.127, Value 0.239, High Risk 0.174, Lotto 0.065. Doc updates (three files): (1) `docs/GLOSSARY.md` rewrote `Composite grade` definition to the 40/40/20 formula; added `Blowout dampening`, `Blowout profile`, `Brier score`, `Calibration`, `KDE`, `KDE window`, `Tier line`, `Tier effectiveness`, plus NBA-specific `Safe tier`, `Value tier`, `High Risk tier`, `Lotto tier`, `Trends Grid` entries. (2) `etl/nba/README.md` rewrote Grade Components section with the new formula (momentum 40% / hr60 40% / pattern 20%) and marked trend/matchup/regression/opportunity as "stored as context only, NOT in composite"; added KDE Tier Lines subsection with full detail on reflection boundary, window selection, tier cutoffs, blowout dampening, and backtest calibration numbers; added four new invariants (3-arg `compute_composite` signature, KDE constants stable, `grade_props_for_date` return shape, blowout dampening market scope); added two open questions on calibration improvements and the Poisson fit for threes. (3) `database/nba/README.md` added "Tier effectiveness" subsection with the full backtest table; updated `common.daily_grades` column descriptions to note ADR-20260423-1 composite removal; added `common.player_tier_lines` invariant; added `odds.game_lines` to the odds tables list since `fetch_game_spreads` reads from it; added open questions for High Risk/Lotto -EV and three-point market calibration. Previously-current docs confirmed unchanged: `web/nba/README.md`, `docs/DECISIONS.md`, `docs/ROADMAP.md`. No code changes this session; grading code already matched docs after the 2026-04-23 backfill. See docs/GLOSSARY.md, etl/nba/README.md, database/nba/README.md, docs/DECISIONS.md ADR-20260423-1.

2026-04-23 [nba][web] Trends Grid post-deploy fix: fallback `teamTricode` from `nba.players` when `nba.daily_lineups` is missing a player. Surfaced on playoff game 0042500123 (NYK @ ATL) where Stage 2 `boxscorepreviewv3` had populated only the 10 starters; bench players like Jordan Clarkson had a tier row but no lineup row, leaving `teamTricode` null and routing them to the UI's 'Other' group. Added a second query against `nba.players` + `nba.teams` for every tier-row player_id; response merges lineup → fallback → null. Verified live on `schnapp.bet/api/tier-grid` across 5 markets (points, rebounds, assists, threes, PRA): 15 player rows for tonight's game now split cleanly 8 NYK / 7 ATL / 0 null / 0 other. Also flagged: for this quieter playoff slate, zero players currently qualify for High Risk (+150) or Lotto (+400) tiers; those columns render as em dashes as designed. Separate operational note: Azure SWA deploys failed with `Upload Timed Out. Polling on deployment.` four times between 17:00-19:30 UTC; pattern matched prior known-transient SWA infrastructure issue; all commits landed on `main` but only retriggers after the backend recovered completed. Commits `d3d6617` (this fix), `8804647` (retrigger that broke the logjam), `f9cee1e` (earlier gameDate fix), `8a8c918` (initial component + wiring), `13ed9fc` (initial route). See web/nba/README.md, docs/DECISIONS.md ADR-20260423-1.

2026-04-23 [nba][web] Shipped NBA Trends Grid as seventh tab on the game page (`/nba?gameId=XXX&tab=trends`). Surfaces `common.player_tier_lines` from ADR-20260423-1: per-player Safe / Value / High Risk / Lotto line values with tier probabilities + prices in tooltips, plus current FanDuel standard line and a scrollable per-game stat history colored against that standard line. URL-driven state: `?stat=` (PTS, REB, AST, 3PM, PRA, PR, PA, RA) and `?window=` (10 / 30 / all). Player rows grouped by team and starter status (Starters / Bench / Inactive) with sticky left-column name and sticky table header. Composite grade color calibrated to the new 40/40/20 formula: 80+ green, 60-79 yellow, 40-59 gray, <40 red. Blowout-dampened players get a warning icon with tooltip. New route `/api/tier-grid` takes `gameId` + `market` + `window` and unions the player list from both `common.player_tier_lines` and `nba.daily_lineups` (tier-without-lineup happens on playoff days when Stage 2 boxscorepreviewv3 has not yet populated bench/inactive). SQL verified live against ATL @ NYK playoff game 0042500123 before commit. Files: `web/components/TrendsGrid.tsx` (new, 397 lines), `web/components/GameTabs.tsx` (added import + Tab union member + render block), `web/app/api/tier-grid/route.ts` (new, 376 lines, shipped in commit `13ed9fc`). ROADMAP.md moved Trends Grid from next-up to shipped. See web/nba/README.md.

2026-04-23 [nba][grading][database] Completed v2 grading backfill under ADR-20260423-1. Ran `grade_props.py --mode backfill --batch 200 --force` directly on the VM (PID 266647, 16:41-18:08 UTC, 87 minutes for 174 dates). Final state: 1,042,113 grade rows re-computed under the new 40% momentum + 40% hit_rate_60*100 + 20% pattern composite formula; `common.player_tier_lines` populated with 190,491 rows (historical) + 498 rows for today's slate (45 players across 3 games, 17 markets). `run_upcoming` wrote today's tier lines automatically on the regular grading schedule; no manual dispatch needed. Tier computation follows ADR-20260423-1: KDE with grade-weighted lookback (15/30/82 games), Scott bandwidth, reflection at 0, normal-dist fallback under 10 games. Tier cutoffs unchanged: safe>=80% probability, value>=58%, high_risk>=28% with +150 market price, lotto>=7% with +400 price and composite>=50. Blowout dampening applied when `nba.games` spread>=10.5 and player is on projected losing team; `fetch_player_blowout_profiles` confirmed reading from `nba.games` (not `nba.schedule`) via join to `nba.player_box_score_stats`. Backfill log preserved at `/tmp/backfill_v2.log` on the VM. See grading/grade_props.py, docs/DECISIONS.md ADR-20260423-1, database/nba/README.md.

2026-04-23 [nba][grading][database] Rebuilt composite grading formula, added KDE tier lines, blowout risk dampening, and new common.player_tier_lines table. Full research session before code: ran grade-outcome correlation analysis (1.04M graded rows) finding composite grade strongly predictive 0-80 but collapsing at 90-100; component decomposition showing momentum_grade and hit_rate_60 account for nearly all predictive lift (28-point and 25-point Won-vs-Lost gaps respectively) while matchup, regression, trend, and all six opportunity grades have gaps of 0.1-1.1 points (effectively zero); blowout profile analysis showing stars like Curry (-18.7 pts), KAT (-14.3), Shai (-13.8) have large blowout-loss deltas while garbage-time players like Mathurin (+7.4) gain; alternate line analysis confirming tier cutoffs (at/below std hits 67.6%, +0.5-2.5 above std hits 31.7%, +3-5 hits 21.8%, +8.5-12 hits 10.5%); spread as blowout predictor (14.5+ spread produces blowouts 49% of the time; big spread + low total produces blowouts 46% with avg margin 21.2 pts). Calibration run (94K rows, scipy KDE): confirmed shift amplitude of -0.019 adds negligible lift (log-loss improvement 0.000012); correct mechanism is grade-weighted lookback window selection, not distribution shifting. ADR-20260423-1: composite reweighted to 40% momentum + 40% hit_rate_60*100 + 20% pattern; matchup, regression, trend, and all six opportunity grades removed from composite mean (stored as context columns only). New compute_kde_tier_lines function: fits gaussian_kde with Scott bandwidth and reflection boundary at 0 on grade-weighted game log window (15 games composite>=80, 30 games 50-79, full season <50, normal dist fallback below 10 games); walks dense 0.5-step grid to find tier cutoffs: safe>=80% probability, value>=58%, high_risk>=28% with +150 or better market price, lotto>=7% with +400 or better and composite>=50. New fetch_game_spreads and fetch_player_blowout_profiles: blowout dampening applied at 50% of historical pts delta when spread>=10.5 for pts/combo markets. New common.player_tier_lines table (one row per player-market-game-date) with safe/value/highrisk/lotto line values, probabilities, and market prices. grade_props_for_date now returns (grade_rows, tier_rows) tuple; run_upcoming, run_intraday, run_backfill all updated to call upsert_tier_lines. Full-season backfill running on VM (PID 266647, started 16:41 UTC) to recompute composite_grade under new formula and populate player_tier_lines for all 174 historical dates. Commits bfb32ee (main change), e60185d (pandas IndexingError hotfix). See grading/grade_props.py, docs/DECISIONS.md ADR-20260423-1, database/nba/README.md.

2026-04-23 [nba][grading][shared][docs] ADR renumbering scheme adopted and opportunity backfill completed. (1) ADR numbering: new ADRs use date-based identifiers `ADR-YYYYMMDD-N`; legacy ADRs 0001-0019 stay on sequential scheme as-is; the pre-existing duplicate `ADR-0017` entry stays per append-only rule; `docs/SESSION_PROTOCOL.md` updated to document the scheme; `docs/DECISIONS.md` preamble updated. (2) ADR-20260422-1: added `--force` flag to `grade_props.py --mode backfill`; removes the `NOT EXISTS` filter so already-graded dates are re-processed via MERGE; existing rows update in place, superseded rows snapshot to `common.daily_grades_archive` before overwrite; `grading.yml` workflow_dispatch gained matching `force` boolean input threaded through env, shell ARGS, and the redispatch step. (3) ADR-20260422-2: VM git push confirmed working via PAT stored at `/home/schnapp-admin/.git-credentials`; prior claim of 403 errors retired. (4) Opportunity backfill: ran `grade_props.py --mode backfill --batch 200 --force` directly on VM (single process, one DB connection) after canceling the slow multi-batch Actions run (workflow 24804870511 had processed only 25 of 174 dates in 3+ hours due to per-batch DB cold-start overhead; direct VM run completed all 174 dates in 62 minutes). Final state: 1,197,594 total rows in `common.daily_grades` across 174 distinct dates (2025-10-21 to 2026-04-22); 1,042,113 rows written by the backfill run. Opportunity coverage: `opp_short_grade` non-null = 965,806 rows; `opp_long_grade` = 965,806; `opp_matchup_grade` = 936,105; `opp_streak_grade` = 983,521; `opp_volume_grade` = 74,663 (threes only); `opp_expected_grade` = 74,663. Blocks and steals correctly 0 across all opportunity columns (no per-player attempt rate). First 6 dates (2025-10-21 to 2025-10-26) have 0% opp_short coverage because season-opening box score history was too thin to produce opportunity estimates; coverage ramps from 58% on 2025-10-27 to 95%+ by late October; recent playoff dates run 41-45% due to fewer games per date. `common.daily_grades_archive` has 0 rows: the archive INSERT in `upsert_grades` fires only when MERGE overwrites a row with a non-null prior value; since all pre-backfill rows had NULL opportunity columns the condition was never met. See grading/grade_props.py, docs/DECISIONS.md ADR-20260422-1, ADR-20260422-2.

2026-04-23 [nba][web] Fixed player page still crashing after React.Fragment fix. Root cause was `/api/player-grades` timing out (15s SWA limit) due to Azure SQL serverless cold-start on DB wake-up; the `Promise.all` treated the 500 response as a fatal error and set `error` state, showing "Error: HTTP 500". Fix: changed the `player-grades` fetch to be non-fatal — non-OK responses and network errors both resolve to `{ grades: [] }` instead of rejecting. The page now loads without prop-coloring data on cold-DB hits and populates coloring on warm hits. `odds.player_props` already has `IX_player_props_player_name` (confirmed present, 8.7M rows); query runs in ~3.3s from VM when DB is warm. Commit `45c0238`. See web/nba/README.md.

2026-04-23 [nba][web] Fixed HTTP 500 on player pages. The game log `map` callback returned a bare `<>` fragment without a `key` prop, causing a React runtime crash on hydration. Fixed by importing `React` as a default import and replacing `<>` / `</>` with `<React.Fragment key={g.gameId}>` / `</React.Fragment>` so the key is on the outermost element returned from the map. Removed the now-redundant `key={g.gameId}` from the inner `<tr>`. Commit `bcad499`. See web/nba/README.md.

2026-04-22 [nba][grading] Added opportunity-based grading: six new per-(player, market) components (`opportunity_short_grade`, `opportunity_long_grade`, `opportunity_matchup_grade`, `opportunity_streak_grade`, `opportunity_volume_grade`, `opportunity_expected_grade`) on `common.daily_grades`. Opportunity is computed per market from already-ingested data — FGA/3PA/FTA in `player_box_score_stats`, `potential_ast` in `player_passing_stats`, `reb_chances` in `player_rebound_chances` — so no new ETL or workflow is needed; grading picks it up automatically on existing schedules. Points opportunity uses per-player trailing shooting percentages (shift-1 rolling 10-game with expanding-season fallback, look-ahead-bias free): `(FGA - 3PA) * r2 * 2 + 3PA * r3 * 3 + FTA * rft`. Threes get two parallel columns: raw 3PA volume trend vs `3PA * r3` expected-made trend. Combo markets (PRA/PR/PA/RA) sum their component opportunities; missing components count as 0 via `sum(min_count=1)`. `fetch_matchup_defense` widened with 5 opportunity ranks (`rank_opp_pts`, `rank_opp_fg3a`, `rank_opp_fg3m`, `rank_reb_chances`, `rank_potential_ast`) and LEFT JOINs `player_passing_stats` and `player_rebound_chances`; `precompute_opportunity_grades` averages the market's component ranks for the matchup grade. `composite_grade` now includes opportunity short/long/matchup/streak in its equal-weighted mean when populated (volume/expected stay out as parallel diagnostic columns for threes). Under rows invert all six via `100 - value`. `_common_grade_data` returns a 7-tuple (new seventh element: `opp_df`); run_upcoming, run_intraday, run_backfill all updated. `ensure_tables` adds the six columns to `daily_grades` and mirrors them onto `daily_grades_archive`; `_ARCHIVE_COLS` extended. `upsert_grades` staging table + INSERT tuple + MERGE widened from 24 to 30 columns. Pandas 3.x compatibility: uses `groupby().transform()` instead of `.apply(group_keys=False)` which drops the grouping column in pandas 3.x. `MARKET_OPP_COMPONENTS` and `OPP_MATCHUP_RANK` added as single source of truth for per-market opportunity definition. Blocks and steals have no opportunity grade (no per-player attempt rate). See ADR-0017, grading/grade_props.py, etl/nba/README.md, database/nba/README.md.

2026-04-22 [nba][web] Two UI fixes. (1) At a Glance matrix view: past-date cells now show a green "W" or red "L" badge instead of a faint background tint when `outcome` is set. The data was already present in the API response; the matrix was only rendering a barely-visible background. Fix in `web/components/PropMatrix.tsx` commit `b31e1b8`. (2) Stats tab period filter persistence: `activePeriods` is now stored in the URL as `?periods=3Q,4Q`. Period buttons write to the URL on change via `router.replace`. Player name links in `StatsTable` include `periods` in the query string. `GameTeamSelector` in `PlayerPageInner` accepts a `periods` prop and includes it in `buildParams` so team pill navigation carries the filter. Player dropdown `select` already uses `searchParams.toString()` so it inherits the fix automatically. Three files changed: `web/components/StatsTable.tsx` commit `9023eb0`, `web/app/nba/player/[playerId]/PlayerPageInner.tsx` commit `f5c66c8`. Also changed `tab` hardcode in `GameTeamSelector` from `boxscore` to `stats` to match the coming-from context. See web/nba/README.md.

2026-04-22 [shared][infra][docs] Tool audit and behavioral rules. Removed MSSQL MCP (ThreatLocker blocks it on corporate machine, WSL env var inheritance also broken). Confirmed working: GitHub MCP, Schnapp Ops (shell_exec, flask_status, workflow_status, live_scoreboard), Filesystem MCP, windows-node-mcp, Desktop Commander, Power BI MCP. Updated docs/CONNECTIONS.md to remove MSSQL MCP entry and document shell_exec as the only DB query path on the corporate machine. Added behavioral rules section to docs/skills/session-protocol.md covering: step-by-step instruction delivery, natural stopping points, fix-first-explain-second, use available tools before asking, no retry on failed approaches, session timeout prevention.

2026-04-22 [nba][grading][web] Fixed archive INSERT ambiguous column error and shipped game log prop expand. Three related changes committed today. (1) `grading/grade_props.py` commit `ca12eb6`: added `dg.` alias prefix to archive SELECT via `_ARCHIVE_COLS_DG` constant, resolving "Ambiguous column name 'grade_id'" (SQL Server 209) caused by bare column names being unresolvable when `daily_grades dg` is joined to the `ranked` CTE which also exposes `grade_id`. (2) `web/app/api/player-grades/route.ts` commit `3a7048b`: added `outcome_name` to response and tightened query to `outcome_name = 'Over'` only, so each row unambiguously represents the posted Over line for that game. (3) `web/app/nba/player/[playerId]/PlayerPageInner.tsx` commit `4f3a485`: `gradeMap` now stores `{ lineValue, outcomeName }` per `(gameId, marketKey)`; `getLineCls` now returns `text-gray-300` (neutral) when no grade exists for a game, and `text-red-400` only when a line was genuinely posted and the stat missed it; clicking any non-DNP game log row toggles an inline expand panel (`GamePropExpandRow`) that lists every market posted for that game with its line value and actual stat in green/red; date and opp cells stop event propagation so they still navigate to the game page; `logColCount` replaces the previous per-branch `dnpColSpan` magic number; new `statForMarket()` helper handles all combo markets explicitly. The dot plot is unchanged — it continues to color dots relative to today's active prop line. See web/nba/README.md.

2026-04-22 [nba][grading] Fixed third archive INSERT crash in `upsert_grades`: added `dg.` prefix to SELECT column list. After the column-list fix in commit `b452682`, the SELECT used bare names from `_ARCHIVE_COLS` while joining `daily_grades dg` to `ranked r`. SQL Server threw "Ambiguous column name 'grade_id'" because `grade_id` appears in both `dg` and `ranked`. Added `_ARCHIVE_COLS_DG` (same list with `dg.` prepended to each column) and used it in the SELECT clause. Commit `ca12eb6`. See grading/ (no component README exists yet).

2026-04-22 [nba][grading] Fixed second archive INSERT crash in `upsert_grades` in `grading/grade_props.py`. After the batch-split fix in commit `fd9c67d`, the archive INSERT failed with "column name or number of supplied values does not match table definition" (SQL Server error 213). Root cause: `SELECT dg.*` expands to `daily_grades` columns in their physical storage order, which diverges from `daily_grades_archive` physical order because both tables accumulated `ALTER TABLE ADD` columns at different points in time. Ordinal positions are set at ADD time and cannot be changed. Fixed by replacing `SELECT dg.*` with an explicit column list (`_ARCHIVE_COLS` module constant) in both the INSERT target and the SELECT source, so the INSERT is immune to physical column order on either table. Commit `b452682`. See grading/ (no component README exists yet).

2026-04-22 [nba][grading] Fixed crash in `upsert_grades` in `grading/grade_props.py`. The archive/prune block after the MERGE had two CTE-based statements (WITH...INSERT and WITH...DELETE) inside a single `conn.execute()` call wrapped in `IF...BEGIN...END`. SQL Server via pyodbc treats each `execute()` as one batch; a `WITH` clause must be the first statement of its batch, so the second `WITH` caused a syntax error. Split into two separate `conn.execute()` calls. The `IF OBJECT_ID` guard was replaced with a scalar count check before the two calls so the logic is the same. Commit `fd9c67d`. See grading/ (no component README exists yet).

2026-04-21 [mlb][docs] Completed `pbix_visual_catalog.json` commit under `etl/mlb/_legacy_powerquery/`. Prior-session commit `c25a3c0` had only 2 of 10 pages (10.8 KB, truncated during an inline paste). Replaced with the full 10-page compact JSON at 61,698 bytes (commit `7549679`), covering all 135 visuals across Game, New, Duplicate of New, Extra, Criteria, EV, MAIN, VS, Proj, and Duplicate of Extra. Each visual records `{id, type, title, refs}` where `refs` is a list of `[kind, table, field]` triples; alias tables (`b`, `p`, `m`, `t`, `u`, `p1`, `p2`) preserved as they appear in DAX projections, resolved via section 3.12 of `MLB_PBIX_REFERENCE.md`. Updated folder `README.md` (commit `ab3c14d`) to move the visual catalog from pending to committed and simplify the regeneration instructions to a one-line `python-docx` command. `mlbStatQueries_full.txt` (225 KB, 4508 lines of verbatim M code) remains uncommitted by design: the parsed `m_query_catalog.json` captures the searchable index (line ranges, endpoint URLs, column counts for all 61 query sections) and a local `git add` is the right path for the raw monolith if it is ever needed, or it can be regenerated from `C:\Users\1stLake\OneDrive - Schnapp\mlbStatQueries.docx` on demand. The main `MLB_PBIX_REFERENCE.md` (35 KB, 551 lines) was committed in an earlier session and required no changes this session. See etl/mlb/_legacy_powerquery/README.md.

2026-04-21 [meta][docs] Added "Where to run which workload" section to `/CLAUDE.md` and then refined host-specific context. Codifies Claude Code as the default editing surface for multi-file and CHANGELOG-bearing edits, claude.ai chat (GitHub MCP) as planning/research only, and introduces the dispatch pattern: claude.ai turns that produce executable work end with a paste-ready Claude Code prompt instead of re-uploading full files via the Contents API. Captured the Windows laptop Claude Code invocation that works around ThreatLocker blocking the `claude.ps1` wrapper: `node "C:\Users\1stLake\AppData\Roaming\npm\node_modules\@anthropic-ai\claude-code\cli-wrapper.cjs"`. Dropped the Mac-as-default-host framing; either machine is a valid Claude Code host. Commits `e4964014` and `a8d2b95`.

2026-04-21 [meta][docs] Added `/CLAUDE.md` at repo root as session context for Claude Code. Covers project identity, session protocol (pointing at `/docs/` as process source of truth), end-of-session requirements, style and tone, a general code structure principle with Flat-Map-Type for Power Query M, split Web and ETL workflows, grep-first context discovery, host-specific context (Windows laptop, MacBook Pro, Azure VM) with Power BI MCP gated to Windows, expected MCP inventory (`schnapp-ops`, `github`, `powerbi-modeling`), live service endpoints, and a "what never to do" list kept to meta rules (code-specific invariants stay in component READMEs). Deliberately excluded MSSQL MCP (not currently connected in Claude Code) and the TTS-punctuation rule from user preferences (Claude Code output is not read aloud). No invariant or roadmap impact. Commit `7179bab`.

2026-04-21 [mlb][web] Shipped MLB EV team view — third ADR-0003 page live (after Game, VS). Three-file change: `web/app/api/mlb-ev/route.ts` aggregates `mlb.player_at_bats` season-to-date over `IX_player_at_bats_batter` for both teams' starters in the selected game, excluding the current game's at-bats. Returns a per-batter summary (BBE, avg/max EV, hard-hit%, avg LA, sweet-spot%, barrel%, HR count, avg xBA) plus full per-at-bat detail for tap-to-expand. Four SQL roundtrips: game info, starters lookup, summary aggregation, per-at-bat detail. Starters come from `mlb.batting_stats` where `batting_order % 100 = 0` when the game has been played; when absent (pre-game), falls back to "top 9 batters by PA in the last 14 days" for each team, flagged via `awayProjected`/`homeProjected` booleans. Barrel defined as `hit_launch_speed >= 95 AND hit_launch_angle BETWEEN 8 AND 32` (simplified barrel proxy, not the exact Statcast definition). Sweet-spot% is rate of balls between 8 and 32 launch angle. `web/app/mlb/MlbEvView.tsx` renders two stacked team tables with color-coded cells (EV velo thresholds 100/95/90, hard-hit% thresholds 50/40/30, barrel% thresholds 15/10/6); row click toggles a single expanded detail sub-table below. Zero-BBE rows dim and don't accept clicks. `React.Fragment` with explicit `key` wraps the row+detail pair to avoid list-key warning inside `<tbody>`. `MlbPageInner.tsx` flipped `ev` from disabled to enabled and added the routing branch. Pre-commit correctness fix: initial route referenced `mlb.players.bats` which does not exist — the VS route uses `bat_side`, renamed accordingly across both starter queries. Also dropped `p.position` from the projected fallback because `mlb.players` has no stable per-player position column and `bs.position` varies per game making it non-aggregatable in the GROUP BY. No new materialized table — the query comfortably fits the indexed access path at current data volumes; if season growth pushes BBE per batter past ~200 the answer will be an ADR-0004 extension, not a runtime change. No new ADR — the projected-lineup fallback is mechanically documented in route comments, not a non-obvious architectural decision. ROADMAP next-up reshuffled: three distinct MLB entries (Player Analysis page blocked on `player_trend_stats`, `player_trend_stats` ETL itself, EV team view page which is data-ready) replace the rolled-up "MLB build" bullet. Completed the third ADR-0003 page; three remain (Proj, Player, Pitcher). See web/mlb/README.md.

2026-04-21 [mlb][web] Shipped MLB VS page — second ADR-0003 page live (after Game). Three-file change: `web/app/api/mlb-bvp/route.ts` reads `mlb.career_batter_vs_pitcher` directly for the ≤18 `(batter, pitcher)` pairs implied by a given gamePk (away batters × home SP + home batters × away SP), with starters sourced from `mlb.pitching_stats.note = 'SP'` and starting lineups from `mlb.batting_stats.batting_order % 100 = 0` in a single UNION ALL. Two SQL roundtrips per request, both hitting indexes (`IX_bvp_pitcher` on one half, the clustered PK on the other). Names joined at read time from `mlb.players`, same pattern as `/api/mlb-atbats`. Returns `available: false` for pre-game or non-Final games that lack SP rows. `web/app/mlb/MlbVsView.tsx` renders two stacked lineup tables (away lineup vs home SP, home lineup vs away SP) with batting-order slot numbers, L/R/S batter hand codes, LHP/RHP pitcher hand labels, and lifetime stats per row (PA, AB, H, HR, RBI, BB, K, AVG, OBP, SLG, OPS, last-faced date). Zero-PA rows stay in batting order with dimmed styling and dashes across stat columns. HR > 0 rows get a faint yellow tint. `web/app/mlb/MlbPageInner.tsx` gained a six-tab view switcher (Game, VS enabled; EV, Proj, Player, Pitcher as "Coming soon" muted labels) driven by `?view=` URL param. Default view is `game`; unknown or disabled values fall back to `game`. Refactored URL construction to a single `buildUrl()` helper that diffs against current searchParams so view state is preserved across date and game changes. Also fixed a pre-existing minor bug: changing dates now drops stale `gameId` from the URL rather than keeping it and forcing the loader to scrub. No ADR — pattern follows ADR-0019 (`/api/mlb-bvp` reads the materialized table directly, no runtime aggregation) and existing route conventions. ROADMAP next-up reshuffled: three distinct MLB entries (Player Analysis page blocked on `player_trend_stats`, `player_trend_stats` ETL itself, EV team view page which is data-ready) replace the rolled-up "MLB build" bullet. Completed the second ADR-0003 page; four remain. See web/mlb/README.md.

2026-04-21 [mlb][etl][database][web] Shipped second ADR-0004 derived entity: `mlb.career_batter_vs_pitcher`. One row per `(batter_id, pitcher_id)` pair with lifetime counts (PA, AB, H, 1B, 2B, 3B, HR, RBI, BB, SO, HBP, SF, TB) plus pre-computed rates (AVG, OBP, SLG, OPS) plus `last_faced_date`. Compound PK `(batter_id, pitcher_id)` clustered; `IX_bvp_pitcher` on `(pitcher_id, batter_id)` for the reverse read path. Source is `mlb.player_at_bats` (not `mlb.play_by_play` directly — at-bats is already at the right grain and indexed on batter_id). Write strategy diverges from ADR-0013/0018: staged MERGE, not direct INSERT, because a `(batter, pitcher)` pair from an earlier flush already has a row requiring UPDATE rather than a duplicate INSERT. Materializer `load_career_bvp_for_games` runs inline in `etl/mlb_play_by_play.py` after `load_player_at_bats_for_games` on each PBP flush: stages affected pairs to `#affected_pairs` by game_pk, joins back to full `player_at_bats` to aggregate into `#stage_bvp`, MERGEs into the permanent table. All counts derive from `result_event_type` via CASE WHEN — no schema change needed to `player_at_bats`. Added `--rebuild-bvp` CLI flag and matching `rebuild_bvp` workflow input on `mlb-pbp-etl.yml`; independent of `--rebuild-at-bats`, the two compose (at-bats first, then BvP). `BVP_AGGREGATE_SELECT` module constant is the single source of truth for the event-type taxonomy, reused by both per-flush loader and full rebuilder. Pre-commit correctness fix: SQL Server does not support `WHERE (col1, col2) IN (SELECT col1, col2 FROM ...)` tuple-IN syntax; rewrote to stage affected pairs in their own temp table and use INNER JOIN. Initial backfill from 384,040 at-bat rows produced 165,550 pairs across 806 batters in ~6 seconds (5 chunks of 200 batters each, merging 45577+46163+39872+32946+992 pairs). No web route built this session — VS page data is now fully in place (both `(batter_id)` and `(pitcher_id)` access paths) but the `/api/mlb-bvp` route and VS page component are deferred to the web build. Added ADR-0019 capturing the three subsidiary decisions (staged MERGE vs pre-diffed INSERT, compound PK vs synthetic string, source = `player_at_bats` vs `play_by_play`) and the tuple-IN anti-pattern. Reduced ADR-0004 missing-entities count from 5 to 4 via initial at-bats shipping; this session reduces it further to 3 (batter context per game, batter projections per game, and trend/pattern+platoon as a potentially shared table). Also corrected a docs bug: `database/mlb/README.md` had previously claimed "four remaining" while listing five items. See etl/mlb/README.md, database/mlb/README.md, web/mlb/README.md, docs/DECISIONS.md ADR-0019.

2026-04-21 [mlb][etl][database][web] Shipped first ADR-0004 derived entity: `mlb.player_at_bats`. One row per completed at-bat (PK `at_bat_id = '{game_pk}-{at_bat_number}'`), materialized from `mlb.play_by_play` via the filter `is_last_pitch = 1 AND result_event_type IS NOT NULL`. Loader `load_player_at_bats_for_games` runs inline in `etl/mlb_play_by_play.py` after each PBP flush; diff runs against `mlb.player_at_bats.game_pk` (not PBP) so partial runs self-heal. Added `--rebuild-at-bats` CLI flag and matching `rebuild_at_bats` workflow input on `mlb-pbp-etl.yml`. Initial backfill from 5,092 existing PBP games produced 384,040 rows in 76 seconds. Iteration: first rebuild denormalized batter_name + pitcher_name from `mlb.players` at write time, which produced 19.8% NULL batter_name and 32.4% NULL pitcher_name because `mlb.players` is current-season-scoped and 983 historical player_ids were missing. Pivoted to ID-only storage with read-time join from `mlb.players`; the joined table has under a thousand rows with a PK on player_id so the cost is negligible. `DDL_DROP_NAME_COLUMNS` in `ensure_table()` idempotently drops the two name columns if present. Swapped `/api/mlb-atbats/route.ts` from the live PBP aggregation query to a direct read of `mlb.player_at_bats` joined to `mlb.players` for names. Parity verified on game_pk=823073 (82 rows both paths). Added ADR-0018 capturing the three subsidiary decisions: inline materialization, diff-against-destination self-heal, ID-only storage. Reduced ADR-0004 missing-entities count from 5 to 4. Unblocked `mlb.player_at_bats` access path for future Player Analysis page work via `IX_player_at_bats_batter` on `(batter_id, game_date)`. See etl/mlb/README.md, database/mlb/README.md, web/mlb/README.md.

2026-04-21 [shared][docs] Added session protocol skill at `/docs/skills/session-protocol.md`. Holds prescriptive execution guidance (start checklist, mid-session signals table, end checklist, mechanical guardrails, known session-boundary failure modes, bounded "when to deviate" list). `/docs/README.md` router updated to list the skill under "Read first every session" and refreshed stale sport-status line (NFL ETL is live, not planning) plus migration-in-progress language (complete as of ADR-0016). `/docs/SESSION_PROTOCOL.md` updated to reference the skill at the top, add the step 4 about executing owed mid-session updates at end-of-session, and remove the stale transition note about legacy files still being in place. Added ADR-0017 capturing the rationale for splitting prescriptive execution guidance out of the canonical protocol definition.

2026-04-21 [nfl][infra][etl][database] NFL ETL is live. Added `.github/workflows/nfl-etl.yml` modeled on `mlb-etl.yml`: Tuesday 09:00 UTC schedule (after MNF + nflverse update), workflow_dispatch with optional `season` input defaulting blank (resolves to `current_nfl_season()`). Runs on `[self-hosted, schnapp-runner]` with `$HOME/venv/bin` activation. Installed runtime deps missing from the runner venv: `nflreadpy-0.1.5` (with transitive polars, pydantic, etc.) and `pyarrow-23.0.1` (required for `polars.to_pandas()`, not pulled as a transitive). First run failed at import, second run produced type-mismatch errors on `nfl.player_game_stats` and `nfl.team_game_stats` because tables already existed from an earlier 2026-03-13 build (PROJECT_REFERENCE had claimed they didn't exist yet). Destination column for `fg_blocked_list` was FLOAT but current data carries a varchar. Dropped both tables and re-ran. Run 3 succeeded in 2:47 with final row counts: games=285, players=24376, player_game_stats=19421, snap_counts=26612, ftn_charting=47316, rosters_weekly=46820, team_game_stats=570. Also noted: the `nfl` schema already contained `seasons`, `teams`, and `pff_*` tables from prior work unrelated to the nflreadpy pipeline. See etl/nfl/README.md.

2026-04-21 [nfl][etl] Replaced deprecated `datetime.utcnow()` with `datetime.now(timezone.utc)` in `etl/nfl_etl.py` ahead of first NFL ETL run. Single call site in `current_nfl_season()` (handoff note said 9, actual count was 1). Import updated to include `timezone`. No behavioral change: both expressions produce the same year/month for the season comparison. See etl/nfl/README.md.

2026-04-20 [shared][docs] Completed /docs/ Step 7: retired legacy /PROJECT_REFERENCE.md and root /CHANGELOG.md. Both files moved to /docs/_archive/ via git mv so commit history is preserved as rename, not delete + add. Archived files got an ARCHIVED banner pointing readers at the active docs. Deleted /docs/MIGRATION_HANDOFF.md (its job was done). Swept 15 files to replace stale "legacy root /CHANGELOG.md" references with /docs/_archive/CHANGELOG.md. Updated /docs/README.md, /docs/SESSION_PROTOCOL.md, /docs/ROADMAP.md to reflect completion rather than in-progress status. Added ADR-0016 documenting the archive (not delete) decision and the rationale (756 lines of genuine engineering history in the old CHANGELOG that was not migrated into /docs/CHANGELOG.md). The documentation restructure described in ADR-0001 is now complete.

2026-04-20 [nfl][docs][etl][database][web] Completed /docs/ Step 6: updated NFL READMEs from the "planning / everything is TBD" skeletons to reflect actual state. etl/nfl_etl.py turned out to be fully written (14 KB, 7 tables via nflreadpy: games, players, player_game_stats, snap_counts, ftn_charting, rosters_weekly, team_game_stats) but never wired to a workflow, so no nfl.* tables exist yet. etl/nfl/README.md now documents schema-from-data inference, clean_df global cleanup, per-table upsert keys, fail-soft error handling, and the local get_engine inconsistency with etl/db.py. database/nfl/README.md documents target schema of all 7 tables with pandas-to-SQL type map and the multi-identifier-system reality of NFL data (gsis_id canonical, pfr_player_id, ftn_player_id, espn_id separate). web/nfl/README.md kept intentionally short because no web surface exists, with specific pointers for where to start when work begins. Added ADR-0014 (schema-from-data is NFL-only) and ADR-0015 (nflreadpy as single source). Primary open question captured in docs: no nfl-etl.yml workflow exists, so the first-ever NFL ETL run is blocked on creating that file.

2026-04-20 [mlb][docs][etl][database][web] Completed /docs/ Step 5: migrated MLB content from legacy PROJECT_REFERENCE.md and from direct source reads into etl/mlb, database/mlb, and web/mlb READMEs. MLB turned out to be further along than the Step 3 skeletons suggested. etl/mlb/README.md documents 7 nightly tables (teams, players, games, batting_stats, pitching_stats, player_season_batting, pitcher_season_stats) plus on-demand mlb.play_by_play. database/mlb/README.md has column-level specs for all 8 tables with PK formats, primary key rationale, and the 5 ADR-0004 derived entities still missing. web/mlb/README.md captures the live /mlb page (date picker, game strip, Box Score + Exit Velo tabs, four /api/mlb-* routes) and the 6-page ADR-0003 roadmap with status per page. Added ADRs 0011 (mlb.games mixed-state), 0012 (pitch-level stays ETL-internal), 0013 (direct INSERT not MERGE for play-by-play).

2026-04-20 [nba][web][infra] Fixed stale /api/games Flask URL. RUNNER_URL was hardcoded to the old VM's IP (20.109.181.21, retired with schnapp-runner on West US 2 B2s_v2), which silently broke live CDN score overlay on the NBA page (routes always fell through to source='db'). Changed to https://live.schnapp.bet to match /api/scoreboard and /api/live-boxscore which already use the Cloudflare-proxied subdomain. Verified via MCP: both /ping and /scoreboard return correctly through the subdomain. Also updated web/nba/README.md and infrastructure/README.md to document the live.schnapp.bet subdomain, the proxied-vs-DNS-only split across all four schnapp.bet subdomains, and a matching invariant that web routes must never hardcode VM IPs.

2026-04-20 [nba][docs][infra] Second audit pass verified against source: Flask binds to 0.0.0.0:5000 (not 127.0.0.1), and /ping is unauthenticated (only /scoreboard and /boxscore check X-Runner-Key). Corrected infrastructure/README.md. /api/games does not drive the game list from Flask; the DB is always source of truth and CDN is overlay-only on today's live games. Corrected web/nba/README.md and added the full API route catalog plus the Central-vs-Eastern timezone split between web and ETL. Flagged /api/games stale Flask IP (20.109.181.21 vs 172.173.126.81) as an open question.

2026-04-20 [nba][docs][infra] Step 4 post-migration audit and fixes: corrected VM size to Standard B1s in infrastructure/README.md; fixed etl/nba/README.md grading file paths (grade_props.py lives in /grading/ not /etl/, and nba_grading.py does not exist) plus rewrote grade component descriptions (momentum_grade is personal lag-1 transitions not log-scaled streaks, pattern_grade is pattern_strength scaled not a reversal rate) and documented bracket expansion plus four grading modes; fixed web/nba/README.md Files section (TodayPropsSection and MatchupsTab are not standalone) and added signal logic section distinguishing player-level DUE from line-level SLUMP; added outcome column to database/nba/README.md common.daily_grades spec; disambiguated three engine variants in etl/_shared/README.md (etl/db.py:get_engine, etl/db.py:get_engine_slow, grading/grade_props.py:get_engine).

2026-04-20 [nba][docs] Completed /docs/ Step 4: migrated NBA content from legacy /PROJECT_REFERENCE.md into etl/nba, web/nba, database/nba, infrastructure, and etl/_shared READMEs. Added ADRs 0005-0010 capturing grading v3, signal redesign, FanDuel-only, two-stage lineup poll, player_line_patterns, and Webshare proxy. Legacy /PROJECT_REFERENCE.md remains until Step 7 retirement.

2026-04-20 [shared][docs] Added /docs/MIGRATION_HANDOFF.md to preserve MLB visual catalog and full migration plan across session boundary. Read before resuming Step 4 or later. File is temporary; delete after Step 7.

2026-04-20 [shared][docs] Completed /docs/ Step 3: added 16 component README skeletons covering ETL, database, web, and infrastructure across all sports. NBA READMEs reference legacy PROJECT_REFERENCE.md as authoritative until Step 4 content migration.

2026-04-20 [shared][docs] Completed /docs/ Step 2: added PRODUCT_BLUEPRINT, CONNECTIONS, GLOSSARY, ROADMAP. Central /docs/ folder is now complete; component README scaffolding starts in Step 3.

2026-04-20 [shared][docs] Created /docs/ foundation: README router, SESSION_PROTOCOL, CHANGELOG (this file), DECISIONS with ADRs 0001-0004. Legacy PROJECT_REFERENCE.md and root CHANGELOG.md remain in place during migration. See /docs/DECISIONS.md ADR-0001.

2026-04-23 [infra][database] Paused Uptime Robot monitor `schnapp-bet-ping` (was pinging https://schnapp.bet/api/ping every 30m). Monitor was preventing Azure SQL Serverless from ever reaching its auto-pause idle threshold, causing the DB to run 24/7 and bill at continuous compute rates (~$181 in April). With the monitor paused, the DB will auto-pause after 60 minutes of idle. Cold start latency (20-60s) is the accepted tradeoff. Resume the monitor if a paying user tier requires warm DB response times.
