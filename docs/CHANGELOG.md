# Changelog

2026-04-25 [nba][grading][analysis] Backfill chain (run 24933461522) at 72/143 ADR-6 dates after ~64 min wall time, slower than handoff projection. Iter 1 wrote 71 dates from Oct 27 forward; iter 2 (PID 327558) currently mid-flight, committing per-date but at slower rate as walk-forward calibration history deepens (each `fit_calibrator(as_of_date=...)` call processes more resolved samples on later dates). Backtest re-run on the 72-date dataset (88,922 resolved tier-line rows via `/tmp/backtest3.py`) shows the Safe undershoot is structural, not a thin-history artifact: Safe 64.7% actual vs 80% design (-15.3 pts gap), worse than the 25-date first-pass 60.5% but the same direction. Predicted 74.8%, actual 64.7%, real return -16.75 cents per dollar. Calibration curve shows overconfidence in high-prob buckets: 0.70-0.80 predicts 73.6% / hits 62.9% (-10.7 pts), 0.80-0.90 predicts 83.5% / hits 74.3% (-9.2 pts), 0.90-1.01 predicts 95.0% / hits 65.8% (-29.3 pts on n=114). Value remains dead-on at 61.1% actual vs 58% design (+1.4 pts) but real return -9.6 cents from vig. HighRisk 22.7% actual vs 28% design (-5.3 pts), real -14.8 cents. Lotto 9.1% actual vs 7% design (+2.1 pts on hit rate) but predicted 12.7% so calibration overshoots, real -27.5 cents. ADR-6 OR-gate cohort split: HighRisk Breakout-only n=303 hits 16.8% / real -6.0 cents (longer prices than Standard which loses -12.2 cents on n=4,675); Lotto Breakout-only n=386 hits 3.6% / real -61.6 cents (catastrophically -EV at +400 prices missing 96% of the time). ADR-6 hit-context columns show minimal signal: HighRisk minutes-match 23.2% vs minutes-below 22.4% (delta 0.8 pts); Lotto minutes-match 8.9% vs minutes-below 10.6% (counterintuitive). Safe EV buckets are anti-predictive at the top: rows with predicted EV +50% or higher (n=216) hit 43.1%, lower than the +0% to +10% bucket which hits 61.9%. The model's high-confidence picks systematically miss. Investigation of high-prob calibration undershoot is the next active work. Held topics still held: walk-forward calibration mechanism (working as designed), 4 EV columns (working as designed), composite reweighting (deferred). See grading/grade_props.py, /tmp/backtest3.py, /tmp/check_backfill.py, docs/DECISIONS.md ADR-20260424-6.

2026-04-25 [infra][security] Replaced failing `github@claude-plugins-official` Claude Code plugin (Copilot-auth, requires Copilot subscription) with a PAT-based GitHub MCP server. Plugin disabled (not uninstalled). New stdio MCP `github` registered at user scope in `~/.claude.json` on the Windows laptop, running `npx -y @modelcontextprotocol/server-github` with `GITHUB_PERSONAL_ACCESS_TOKEN` env var. Token: fine-grained PAT named `Claude Code MCP - sports-modeling`, all SchnappAPI repos, Actions/Contents/Issues/Pull requests/Workflows read+write + Metadata read, expires 2027-04-25. Local-only credential — does not need to rotate alongside `GITHUB_PAT`/`GH_PAT`. Mobile (claude.ai) continues to use the separate `Claude MCP` classic PAT via the GitHub connector. See /docs/CONNECTIONS.md Personal Access Tokens.